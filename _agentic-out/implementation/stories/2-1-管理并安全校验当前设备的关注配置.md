---
artifact_kind: story
status: done
delivery_profile: standard
delivery_scope: windows-first
source_story: '2.1'
blocking_condition: ''
followup_review_recommended: true
---

# Story 2.1：管理并安全校验当前设备的关注配置

状态：done

## Story

作为 AI 开发者，  
我希望管理关注赛道、关键词、排除词、来源偏好和刷新策略，并在保存前理解错误或漏报风险，  
以便真实来源同步围绕我的技术判断需求可靠运行。

## 验收标准

### AC1：管理赛道并版本化保存到当前设备

**Given** 用户添加、修改、启用或停用关注赛道  
**When** 提交配置  
**Then** 核心验证名称、状态和重复冲突，并把有效配置保存到当前设备  
**And** 保存返回稳定版本；失败时保留全部输入、焦点和滚动位置。（FR4、ARCH-18）

### AC2：阻断性无效配置不得保存

**Given** 用户编辑关键词、排除词、来源、刷新周期、可信度或阈值  
**When** 配置命中表达式无法解析、数值或日期越界、下界高于上界、来源地址或标识无效或协议不受支持中的任一固定无效类别  
**Then** 系统禁止保存，并指出受影响字段、无效类别和修正方式  
**And** 焦点移至首个错误字段，错误与控件程序化关联且不只依赖颜色。（FR5、FR62、UX-DR28）

### AC3：过窄配置必须明确解释并确认

**Given** 配置可以解析但命中“全部来源停用”或“当前设备全部高可信候选被过滤”  
**When** 用户准备保存  
**Then** `RuleImpactPreview` 说明受影响条件和可能漏报的后果  
**And** 用户可以返回修改或明确确认保存；未确认前不得提交。（FR62、UX-DR6、UX-DR28）

### AC4：无风险配置直接保存

**Given** 配置没有阻断错误且未命中过窄风险  
**When** 用户保存  
**Then** 系统直接保存版本化配置  
**And** 不显示风险确认层或要求额外确认。（FR62、UX-DR28）

### AC5：确认回执不可重放或跨配置复用

**Given** 用户在过窄风险提示中确认保存  
**When** 配置在确认前被修改，或确认回执已过期、被使用或不匹配当前规范化配置  
**Then** 系统不得接受旧确认，并重新执行校验  
**And** 确认回执只绑定当前配置 hash 和验证器版本，不得持久化为永久绕过凭据。（ARCH-28）

### AC6：保存后续可消费的规则但不提前产生副作用

**Given** 用户配置来源可信度、提醒阈值、免打扰时段和提醒频率上限  
**When** 配置通过校验并保存  
**Then** 系统保存可供后续筛选和通知 Epic 使用的版本化规则  
**And** 本 Story 不生成通知、不依赖未来通知实现，也不静默修改历史情报。（FR6）

### AC7：重启与离线时仍可管理本机配置

**Given** 用户重新启动应用或处于离线状态  
**When** 再次打开设置或保存只需本地事务的有效配置  
**Then** 已保存配置保持可用，适用修改仍可离线保存  
**And** 界面明确说明配置只影响当前设备。（FR5、UX-DR9）

### AC8：完整表单状态、脏表单保护和键盘操作

**Given** 表单处于初始、已修改、校验中、警告、阻断错误、保存中、成功或保存失败状态  
**When** 用户使用鼠标、触控或键盘操作  
**Then** 状态、错误和保存结果具有稳定名称及非颜色提示  
**And** 离开脏表单前提示用户，保存失败不清除焦点、滚动位置或未提交输入。（UX-DR17、UX-DR19、UX-DR20）

### Windows-first 验收边界

- 本轮实施共享 Rust core、Windows Tauri/DesktopApi 和 Windows UI；Apple/iPhone/iPad/Android 的等价平台实现及运行证据继续 Deferred/N/A。
- 当前产品不承诺盲人屏幕阅读器支持；仍必须保证键盘可完成、可见焦点、无焦点陷阱、稳定控件名称、非颜色状态与等效视口可用。
- 真实 Windows 100%/125%/150%/175%/200% × 浅/深主题矩阵只在专用发布机或隔离 VM 执行；本 Story 不修改开发者日常系统主题、缩放、注册表或权限。
- 完成结论只能声明 `Windows milestone PASS/FAIL`，不得宣称跨平台 MVP 完成。

## Tasks / Subtasks

- [x] Task 1：建立版本化关注配置与共享校验合同（AC1–AC6）
  - [x] 在 `radar-core` 定义权威 `AttentionConfigurationV1`、`ConfigurationViewV1`、`ValidateConfigurationInputV1`、`ConfigurationValidationResultV1`、`SaveConfigurationInputV1`，每个独立 DTO 携带 `contract_version`；JSON 使用 `snake_case`，未知枚举 fail closed，缺失可选值显式 `null`。
  - [x] 配置合同覆盖：稳定赛道 ID/名称/启停、包含表达式、排除表达式、来源偏好与稳定标识、刷新周期、来源可信度、提醒阈值、免打扰时段和提醒频率上限；所有 exact 长度、数量、数值/日期范围必须先集中声明为 core 命名常量并镜像到 schema/fixture，不得在 React、Tauri 或测试工厂散落另一套范围。
  - [x] V1 固定边界：1–32 个赛道，名称 trim 后 1–64 Unicode scalar；1–64 个来源偏好，标识 1–2048 bytes；刷新周期 15–10080 分钟；可信度、可信过滤上下界和提醒阈值均为 0–100 闭区间；高可信定义为 `trust >= 80`；启用提醒上限时为 1–100 次/滚动 24 小时，禁用时值必须为 `null`。
  - [x] 免打扰使用本机显示时区的 `HH:mm`（00:00–23:59）与显式 enabled；启用时 start/end 不得相同，允许跨午夜，DST 的实际调度留给通知 Story。可选规则生效窗使用 RFC3339 UTC `active_from`/`active_until`，两者同时存在时必须 `from <= until`；非法日期/offset 属 `value_out_of_range`，倒置属 `lower_bound_above_upper_bound`。
  - [x] 固定阻断代码仅为 `expression_unparseable`、`value_out_of_range`、`lower_bound_above_upper_bound`、`invalid_source_or_unsupported_protocol`；每项返回稳定 `field_path`、`code`、`message_key`/修正提示键和确定性顺序。
  - [x] 固定风险代码仅为 `all_sources_disabled`、`all_high_trust_candidates_filtered`；只有当前设备存在可评估候选时才判定第二类风险，不能把空数据集推断为“全部被过滤”。
  - [x] Phase 1 表达式 grammar 固定为：空字符串表示无约束；term 可为无空白 Unicode token 或双引号 phrase；支持 ASCII 大小写不敏感的 `AND`/`OR`、括号（AND 优先于 OR），仅允许反斜线与双引号转义；排除语义使用独立 exclude expression，不支持 `NOT`。每式 ≤512 UTF-8 bytes、≤32 terms、term 1–64 Unicode scalar、括号深度 ≤4；悬空运算符、未知转义、空 term、未闭合引号/括号或超限统一 `expression_unparseable`。解析必须是有界确定性 parser，不使用可能灾难回溯的正则。
  - [x] 生成唯一权威 fixtures：`contracts/fixtures/configuration-validation/blocking/` 四类各至少一个、`narrowing/` 两类各一个、`valid/` 至少一个；所有值和声明范围固定、无随机/系统时间/网络依赖。
  - [x] 同步 Rust manifest、可读 schema、error snapshot、golden fixtures、当前 radar-ffi 共享 DTO/error 映射和 TypeScript guard；Apple/Android 生成绑定与运行证据继续 Deferred，不得借此扩张移动端调用面。不得依赖同源自比或只检查文件存在。

- [x] Task 2：实现规范化、两通道验证和安全 receipt（AC2–AC5）
  - [x] 新建 `crates/radar-core/src/domain/rules/configuration_validation.rs`，作为规范化、解析、范围/冲突验证、风险评估、canonical bytes/hash 和 receipt identity 的唯一权威；平台不得重算业务含义。
  - [x] V1 Unicode 规范固定为：文本 trim Unicode whitespace 后保留原 code points（不做 NFC/NFKC），赛道重名和 term 匹配使用 Rust Unicode lowercase，`AND`/`OR` 运算符及协议/来源 kind 仅做 ASCII case-insensitive；列表按 canonical UTF-8 bytes 排序去重。该规则用于生成唯一 canonical bytes 和 `normalized_config_hash`；使用明确锁定的抗碰撞 hash，并在 receipt 校验时同时比较 canonical identity，不得把 FNV、时间、PID 或原子序列当作安全 hash。hash/receipt 不包含 secret，日志不得记录完整配置文本。
  - [x] `ConfigurationValidationResultV1` 明确返回 `blocking_errors`、`narrowing_risks`、`validator_version`、`normalized_config_hash` 和可选 `validation_receipt`；阻断与风险不能同时出现，正常配置不返回 receipt。
  - [x] `all_high_trust_candidates_filtered` 的生产输入固定为 core-owned `ConfigurationCandidateContext`：只读取 `data_origin='real'` 的现有 `intel_items` + `item_provenance`，V1 按现有 `source_kind` 取得 kind-level trust（逐 endpoint/source ID trust 留给真实来源表 Story），并以同一 core parser/predicate 比较过滤前后集合；demo 永远排除。过滤前高可信真实候选 >0 且过滤后为0才产生风险；当前无真实候选时不产生此风险。测试通过 repository seam 注入固定 real candidates，不允许 UI 或 fixture 自报风险。
  - [x] receipt 使用 32-byte CSPRNG 后 base64url-no-pad 的进程内不透明句柄；canonical hash 固定 SHA-256 lowercase hex。允许在项目 `Cargo.toml` 精确锁定经审查的直接依赖（优先复用当前 lockfile 中 `getrandom 0.3.4`、`sha2 0.10.9`）并更新项目 `Cargo.lock`；禁止全局安装、FNV/DefaultHasher、时间、PID 或原子序列。
  - [x] receipt TTL 固定 5 分钟，最大 64 个未消费记录；签发前清理过期项，满容量后按签发序 FIFO 淘汰最旧 available 项，被淘汰句柄稳定返回 stale。测试通过注入 Clock/Entropy，不等待真实时间。
  - [x] 保存时始终重新规范化和验证；伪造、过期、已消费、风险集合变化、validator 变化、配置变化统一返回 AppError `validation.stale_validation_receipt`（表单级稳定原因 `stale_validation_receipt`），保留 draft 并重新进入校验，不映射为 internal。
  - [x] 保存入口顺序固定：先查 idempotency key；同 key+同 request fingerprint 直接重放已提交的不可变响应，不再校验已消费 receipt；同 key+不同 payload 拒绝。仅新 key 才检查 revision、重新验证并 reservation receipt。现有 `DemoState` 串行 mutex 内完成 DB commit 后、下个命令可进入前将 receipt 标记 consumed；commit/约束失败释放 reservation 给同一 intent 重试。进程重启使所有 receipt 失效；不得提供 `forceSave`、`force: true`、永久确认偏好或平台布尔绕过。

- [x] Task 3：以单一 SQLite 所有者保存不可变配置版本（AC1、AC4–AC7）
  - [x] 从当前 schema v3 演进到 v4；fresh、v1/v2/v3→v4 具有相同表/列/约束，逐字段保留 legacy demo、provenance、FTS、setup progress 和 Story 1.8 的有效默认选择。
  - [x] v4 固定新增 `configuration_versions(version INTEGER PRIMARY KEY CHECK(version>=1), validator_version TEXT NOT NULL, normalized_config_hash TEXT NOT NULL, configuration_json TEXT NOT NULL, created_at_ms INTEGER NOT NULL)`、`configuration_current(singleton_id INTEGER PRIMARY KEY CHECK(singleton_id=1), version INTEGER NOT NULL UNIQUE REFERENCES configuration_versions(version))`、`configuration_idempotency(idempotency_key TEXT PRIMARY KEY, request_fingerprint TEXT NOT NULL, response_version INTEGER NOT NULL REFERENCES configuration_versions(version), response_json TEXT NOT NULL)`；同一单调 `version` 同时作为并发 `revision`，避免双计数漂移。幂等表事务内仅保留最近64条。
  - [x] 继续使用现有 `DemoStore`/共享应用存储和 `settings_metadata` 边界；v4 后完整业务配置只以 `configuration_versions/current` 为权威，`settings_metadata` 仅保留迁移兼容/非重叠元数据，不得建立第二 SQLite、第二迁移器、平台数据库连接或长期平行配置副本。
  - [x] 首次 v4 migration 从 Story 1.8 的 `settings_metadata` keys `track_ids`、`refresh_cadence`、`ai_data_disclosure_acknowledged` 生成 configuration version 1；`source_example_ids` 继续作为 inert setup metadata/来源类型模板，绝不迁移成已启用真实 endpoint、订阅或“已同步”状态。无用户行时使用 core-owned effective defaults；不能无损映射则停止迁移并返回稳定 migration error。
  - [x] v4 后 `get_setup_progress` 从 configuration current 投影已保存赛道与刷新值；`source_examples` 始终只展示 demo/template，真实 `source_preferences` 使用独立 DTO 字段且不得塞入 `source_example_ids`。静态 defaults fixture 只提供恢复默认候选；SetupProgressV1 的 track options 改为动态当前+默认候选集合，TS guard/golden 校验稳定 ID/唯一性而非“必须属于3个静态ID”。
  - [x] `SaveSetupStepInputV1` 增加 `expected_configuration_revision`。`save_setup_step` 在同一 Immediate transaction 重新读取 current，只 patch 该 setup step 拥有的赛道或刷新字段，并同时提交 configuration version + setup progress；revision 不匹配返回稳定 conflict，绝不以 last-write-wins 覆盖 `/rules` 的并发修改。source_examples/AI disclosure 只改其独立 setup metadata，不改真实 source preferences。
  - [x] `get_configuration()` 返回稳定 config version/revision、validator version、规范 hash 和已保存视图；每次成功保存追加不可变版本并原子切换 current，供未来 rule evaluation/sync 引用；读取、预览与失败验证不得创建版本。
  - [x] `save_configuration()` 携带 `expected_revision` 与 `idempotency_key`；同一 intent 的超时/响应丢失重试复用同一 key并返回同一响应，配置改变才生成新 key。revision 竞争只有一个赢家，过期 revision 返回 conflict，禁止静默覆盖。
  - [x] 使用 checked arithmetic、真实 UTC 时间和显式事务；存储/约束/序列化任一步失败时配置、当前指针、revision、receipt 和幂等记录全部保持一致。幂等历史必须有界。
  - [x] 启动时验证 v4 必需对象、列、单例 current、FK 和关键约束；损坏或迁移失败不得等到用户保存时才暴露。当前 Story 不虚构完整只读迁移诊断：独立 health 保持可用，所有依赖 DemoStore 的页面收到稳定 migration/storage error，旧数据库不被继续写。

- [x] Task 4：扩展精确 Tauri command 与 DesktopApi 边界（AC1–AC7）
  - [x] 新增精确异步命令 `configuration_v1`、`validate_configuration_v1`、`save_configuration_v1`；阻塞 SQLite 工作继续进入现有受控 blocking executor，使用统一 `CommandErrorV1`、panic containment 和 DesktopApi 10 秒有界等待。
  - [x] 所有命令只接收 owned DTO；禁止 generic execute、通用 SQL/文件/shell/HTTP/URL command，也不得新增 WebView capability 或网络权限。
  - [x] 扩展唯一 `DesktopApi` 与 `tauri-desktop-api.ts`：校验 exact keys、safe integer、contract/validator version、枚举、唯一字段 path、错误/风险互斥、receipt 出现条件、hash、revision 和请求/响应 identity；未知或矛盾 payload fail closed。
  - [x] timeout 与 contract corruption 使用不同稳定错误；mutation 超时后先查询 current revision/幂等结果，不能盲目用新 key 重发。迟到 query 不得覆盖更高 revision 的保存结果。
  - [x] 同步 release handler allowlist、Tauri integration/release-surface tests、xtask boundary/mutation gate、Playwright command mock 和 DTO factory；scanner 必须解析真实结构，不能靠可伪造源码子串放行。
  - [x] 集中 query keys只用于 `configurationKeys.current(apiKey)`；validation 使用 mutation，不在 core 返回前让 React 计算或命名“normalized hash”。若保存后缓存验证结果，只能使用 core 返回的权威 hash；draft 不进 Query cache，成功后用权威 view 更新/精确失效 current/setup 相关查询。

- [x] Task 5：实现 Windows “雷达规则”入口、表单和 RuleImpactPreview（AC1–AC8）
  - [x] 新增 `/rules` 作为应用一级目的地，并在设置根页提供相关入口；不要把完整规则编辑器塞回 `ProgressiveSetupGuide`。Setup 中保存相同字段时必须复用同一个 core validator/save API。
  - [x] 在唯一目录 `apps/windows/src/features/configuration-validation/` 实现规则表单与 `RuleImpactPreview`；复用现有 Router、QueryClient、Button、设计 tokens、forced-colors 和 reduced-motion 基线，不为 DTO 引入 Zustand/localStorage。
  - [x] 表单 draft 仅在组件局部状态；初始化从 core view 创建。状态机固定为 `pristine`、`dirty`、`validating`、`blocking_invalid`、`narrowing_risk`、`saving`、`saved`、`save_error`，不得用互相矛盾的布尔组合。
  - [x] 标签位于控件上方，说明和字段错误在下方并通过稳定 ID/`aria-describedby` 关联；placeholder 不替代 label。阻断后聚焦确定性首错，修复后重新验证且不弹风险 Dialog。
  - [x] 风险层使用 warning 而非 destructive，明确“返回修改 / 理解风险并保存”；打开后焦点进入、Tab 不逃逸、Esc 关闭并返回触发项。draft 任意规范化变化立即丢弃旧 receipt。
  - [x] 正常配置直接保存。保存期间按钮尺寸稳定且防重复提交；成功持续显示“已保存，将在下一轮同步生效；历史情报未改写”；失败内联保留 draft、首选焦点、滚动和重试 intent，不只显示短暂 Toast。
  - [x] 在现有 Declarative `BrowserRouter` 上建立应用级 dirty-navigation guard，覆盖应用 Link/程序化导航、浏览器 back/forward（popstate）和窗口关闭（beforeunload）；不得为了 `useBlocker` 偷换 Data Router、loader/action。取消保留输入/焦点/滚动，确认离开不自动保存；已有主流上下文不得因访问规则页被重建。
  - [x] 页面持续显示“仅影响此 Windows 设备；无云备份或跨设备同步”。本 Story 不申请通知权限，不访问 Credential Manager，不执行网络、AI 或来源同步。

- [x] Task 6：覆盖核心、契约、Windows UI 和真实候选边界（AC1–AC8）
  - [x] Rust/fixture 测试覆盖四类 blocking、两类 narrowing、valid 直存、名称空白/长度/重复、启停冲突、表达式、范围边界、来源协议/标识、规范 hash 稳定与当前候选为零时不误报风险。
  - [x] receipt 测试覆盖成功单次、无 receipt 拒绝、伪造、过期、重复、配置变化、validator 变化、风险集合变化、存储失败后仍可重试、重启失效和有界容量；使用注入时钟/熵或固定测试 seam，不等待真实时间。
  - [x] 数据库测试覆盖 fresh/v1/v2/v3→v4 migration、setup/defaults 逐字段无损、reopen/离线读取、不可变版本、expected revision 并发、同 key 同/异 payload、幂等响应优先于已消费 receipt、response-lost 恢复、事务回滚、revision 溢出、损坏 schema/JSON 与启动合同。
  - [x] transport/TS guard 覆盖缺字段、额外字段、未知枚举、重复/无序 field path、errors+risks 同时存在、receipt 出现在 blocking/valid、hash/validator mismatch、unsafe integer、requested/returned mismatch、panic 后恢复和错误脱敏。
  - [x] Vitest 覆盖初始 loading/error、dirty/validating/blocking/risk/saved/save_error、首错聚焦/关联、两类 warning、normal 无 Dialog、stale receipt 重校验、普通失败同 key 重试、空数值原样保留、dirty 导航、Esc/焦点恢复；颜色/缩放/读屏真实矩阵按用户批准的最小闭环范围不作为本阶段门禁。
  - [x] Playwright 新增 `story-2-1-configuration-validation.spec.ts`：P0 覆盖 blocking/warning/valid 三通道；另覆盖赛道增改启停、浏览器 reload 后本地状态恢复、保存后下一轮生效/历史不改写以及跨 reload 累积的 external calls=0。真实进程重启由 native smoke 负责。
  - [x] 扩展项目隔离 native smoke：真实 Tauri 查询/验证/保存、键盘首错、blocking 未落盘、风险确认前未落盘、返回修改焦点、确认后重启恢复、候选及其后代进程清零；只记录实际测量值，未测权限/主题/缩放须写 `not_measured`，不得硬编码通过。
  - [x] 因生产 core/UI/schema 变化，重建 MSVC release candidate，并按用户批准口径重跑30次冷启动（P95≤5秒）；证据必须绑定新 candidate/source SHA，`AI_SUBSCRIBE_BENCHMARK_DATA_DIR` 固定到项目 `target/...`，不得访问用户 Roaming 数据或沿用 Story 1.8 候选证据。
  - [x] 完成项目隔离的 frozen install、format/lint/typecheck/Vitest/Playwright/build、Rust fmt/Clippy/workspace tests、xtask contracts 和 MSVC release；禁止全局 Python/Node/Rust 安装，不改变系统设置。

### Review Findings

#### 第一组：Rust Core、配置验证、SQLite v4 与迁移

- [x] [Review][Patch] 补齐所有 changed behavior 的 Requirement Change Log，恢复 AC → Task → Test/evidence → RCL 完整链 [_agentic-out/implementation/stories/2-1-管理并安全校验当前设备的关注配置.md:255]
- [x] [Review][Patch] Receipt 除 SHA-256 外还必须保存并比较 canonical identity，相关测试不得只篡改 hash 代理验证 [crates/radar-core/src/domain/rules/configuration_validation.rs:37]
- [x] [Review][Patch] 使用结构化 URL 解析校验 HTTP(S) scheme、host 和非法形态，并支持 scheme ASCII 大小写 [crates/radar-core/src/domain/rules/configuration_validation.rs:321]
- [x] [Review][Patch] v3→v4 必须在单事务中创建 schema 并生成已完整验证的 version 1；未知 cadence、空/重复/非法 legacy 值须 fail closed [crates/radar-core/src/application/demo.rs:484]
- [x] [Review][Patch] RFC3339 active window 必须按真实 instant 比较，不能用不同小数精度的字符串字典序判断 [crates/radar-core/src/domain/rules/configuration_validation.rs:353]
- [x] [Review][Patch] 配置保存须在同一 IMMEDIATE 事务内读取/比较 revision，并把多连接竞争稳定映射为 revision conflict [crates/radar-core/src/application/configuration.rs:217]
- [x] [Review][Patch] 启动 schema 审计须验证 v4 CHECK、UNIQUE、FK 与单例约束，而不只是表名和列名 [crates/radar-core/src/application/demo.rs:1205]
- [x] [Review][Patch] 风险校验的系统熵失败必须返回稳定错误，不能返回带风险但无 receipt 的矛盾成功 DTO [crates/radar-core/src/domain/rules/configuration_validation.rs:212]
- [x] [Review][Patch] Receipt 容量只计算 available 项；消费项应及时移除，不能挤出仍有效的较早 receipt [crates/radar-core/src/domain/rules/configuration_validation.rs:92]
- [x] [Review][Patch] Setup 幂等重放必须返回首次提交的不可变响应，而非随后变化的当前进度 [crates/radar-core/src/application/setup.rs:165]
- [x] [Review][Patch] Setup 读取、提交响应和幂等快照必须来自一致事务快照，避免 revision/content 撕裂及提交后被后续写覆盖 [crates/radar-core/src/application/setup.rs:180]
- [x] [Review][Patch] Track ID、名称与重复冲突必须返回准确字段路径，不能把 ID 错误统一指向 `.name` [crates/radar-core/src/domain/rules/configuration_validation.rs:274]
- [x] [Review][Patch] `get_configuration` 每次读取须复核 validator、规范化结果与 hash，运行期间出现合法 JSON 级损坏也应 fail closed [crates/radar-core/src/application/configuration.rs:66]
- [x] [Review][Patch] 修正 setup 未知枚举负例，使除目标 enum 外的所有必填字段合法，避免测试因缺字段产生假阳性 [crates/radar-core/tests/setup_progress.rs:337]
- [x] [Review][Patch] 补齐 Task 6 明列的边界、损坏、溢出、迁移回滚与无效 HTTP(S) 直接测试，未覆盖前不得保持 Task 完成声明 [crates/radar-core/tests/configuration_validation.rs:98]
- [x] [Review][Patch] 冷启动证据须绑定 configuration/domain/DTO/fixture 关键源码 SHA；未测系统主题、缩放和权限字段必须写 `not_measured` [scripts/windows-configuration-smoke.ps1:1]

#### 第二组：IPC、共享合同、Tauri release surface 与 DesktopApi

- [x] [Review][Patch] 把 DemoStore 首次打开/迁移也纳入 redacting panic hook 和 `catch_unwind`，避免初始化 panic 泄漏并毒化状态锁 [apps/windows/src-tauri/src/commands/mod.rs:100]
- [x] [Review][Patch] DesktopApi error guard 须验证 exact keys、稳定 code/category/message/retryability 映射与不透明 correlation ID [apps/windows/src/lib/desktop-api/desktop-api.ts:379]
- [x] [Review][Patch] Configuration wire guard 须镜像 track/source/expression/identifier/schedule/cap 的完整边界、唯一性和交叉不变量 [apps/windows/src/lib/desktop-api/desktop-api.ts:559]
- [x] [Review][Patch] Blocking/risk DTO 须把每个 code 与固定 message/condition/consequence key 绑定，不得接受自相矛盾的合法枚举 [apps/windows/src/lib/desktop-api/desktop-api.ts:683]
- [x] [Review][Patch] Windows 边界扫描须纳入 `.mts`/`.cts`/`.jsx`，并用 mutation tests 证明不能以扩展名绕过 [crates/xtask/src/contracts.rs:448]
- [x] [Review][Patch] raw invoke 门禁须全面拒绝 DesktopApi 外的 `__TAURI_INTERNALS__` 别名/解构/可选调用绕过 [crates/xtask/src/contracts.rs:618]
- [x] [Review][Patch] configuration golden 不得只与 xtask 内嵌副本同源自比；xtask 须读取唯一 fixture 并执行生产 validator [crates/xtask/src/contracts.rs:256]
- [x] [Review][Patch] 共享 configuration fixture 须穿过实际 Tauri command helper 的 serde input→core→serialized output，不得只验证命令字符串 [apps/windows/src-tauri/src/commands/mod.rs:251]
- [x] [Review][Patch] Contract manifest 的 configuration 边界/code/receipt 必须由 validator 命名常量生成，不得维护第二套手写规则 [crates/radar-core/src/contracts/manifest.rs:43]
- [x] [Review][Patch] `ConfigurationViewV1` 返回值须与已验证请求的规范 hash/配置关系一致，不得只验 64 位 hex 外形 [apps/windows/src/lib/desktop-api/tauri-desktop-api.ts:177]
- [x] [Review][Patch] Setup option ID guard 须接受 core 合法的 `-`/`:`/`.` 自定义 track ID，并在 manifest 声明字符集 [apps/windows/src/lib/desktop-api/desktop-api.ts:769]
- [x] [Review][Dismissed] Demo V1 保持已发布的 additive compatibility；既有直接测试要求接受未携带后加 evidence 字段的 legacy summary，因此不改为 exact-key/强制 importance/ai_status [apps/windows/src/lib/desktop-api/desktop-api.ts:405]
- [x] [Review][Patch] Manifest 须登记 attention track/source/quiet hours/frequency 与 setup defaults/options/saved config/provenance 等嵌套 DTO，避免子结构漂移 [contracts/schemas/contract-manifest-v1.json:72]
- [x] [Review][Patch] xtask required fixtures 须纳入 setup golden 和 blocking/narrowing/valid configuration suites，不得让共享案例静默删除 [crates/xtask/src/contracts.rs:263]
- [x] [Review][Patch] FFI/命令跨层证据须包含 blocking 枚举和 narrowing receipt 结构，不得只消费无风险 happy path [crates/radar-ffi/tests/boundary.rs:50]
- [x] [Review][Patch] `.env.example` 只能在根目录作为占位模板，任意子目录同名文件不得绕过敏感制品门禁 [crates/xtask/src/contracts.rs:425]

#### 第二组审查中已驳回的非缺陷

- [x] [Review][Dismissed] Tauri 写命令超时代表结果未知，但 setup/configuration 已强制同 intent 幂等键重放并先查已提交响应；不把无法取消的本地 IPC 误判为丢写缺陷。
- [x] [Review][Dismissed] 受信本地打包 WebView 在严格 CSP/无远程资源下不是对抗本进程内存 DoS 的安全边界；Rust 仍在持久化前强制数量/长度上限。
- [x] [Review][Dismissed] `saved_fields_version` 在 V1 的唯一已发布非空值就是 1；未来版本演进须先改共享合同，不提前放宽当前 guard。

#### 第三组：Windows 配置 UI、路由、状态与交互

- [x] [Review][Patch] 首次 configuration query 失败时必须先命中 error/retry，不得因 draft 为 null 永久显示 loading [configuration-editor.tsx:201]
- [x] [Review][Patch] 来源行 React key 不得包含正在编辑的 identifier，避免每个字符都卸载并丢失焦点 [configuration-editor.tsx:354]
- [x] [Review][Patch] validating/saving 必须有进程内互斥和 disabled 保存按钮，禁止多幂等键并发保存 [configuration-editor.tsx:699]
- [x] [Review][Patch] stale receipt 要清除旧 intent 并重新校验；revision conflict 还要刷新当前 revision，其他未知结果错误才保留同一 intent 重放 [configuration-editor.tsx:151]
- [x] [Review][Patch] DesktopApi provider 切换必须重建编辑 session，不得把旧设备 draft/迟到响应保存到新 provider [configuration-editor.tsx:32]
- [x] [Review][Patch] draft 须绑定开始编辑时的 base revision，后续 query refetch 不得让旧 draft 借用新 revision 静默覆盖 [configuration-editor.tsx:43]
- [x] [Review][Patch] 保存成功须 invalidate `setupKeys.progress(apiKey)`，避免 setup 无限 stale cache 显示旧赛道/节奏 [configuration-editor.tsx:150]
- [x] [Review][Patch] dirty guard 须包含 validating/saving，并按 history direction 恢复被取消的 back/forward，不得无条件 `go(1)` [configuration-editor.tsx:44]
- [x] [Review][Patch] Core 须返回准确数值/source trust field path，使首错焦点和 `aria-describedby` 真正命中生产响应 [configuration_validation.rs:379]
- [x] [Review][Patch] 阻断/风险 UI 须将稳定 key 映射为可理解的字段、修正方式、影响条件和漏报后果，不直接显示内部 code/key [configuration-editor.tsx:681]
- [x] [Review][Patch] 来源偏好添加上限须与 core 64 一致，不得在 32 时提前阻断 [configuration-editor.tsx:488]
- [x] [Review][Patch] 数值输入必须保留用户的原始空字符串，校验失败不得把空输入改写为 0 [configuration-editor.tsx:431]
- [x] [Review][Patch] narrowing dialog 显示时背景表单须 inert；确认保存失败须返回保存触发点并显示持久错误 [configuration-editor.tsx:722]
- [x] [Review][Patch] `/` 须有显式 route，不得让有效首页持续命中 wildcard 自重定向 [app-router.tsx:50]
- [x] [Review][Patch] Setup 写入期间不得允许返回设置后再被迟到 `navigate("/")` 拉走；卸载后 onSuccess 不导航 [progressive-setup-guide.tsx:75]
- [x] [Review][Patch] Setup 幂等键必须用高熵唯一值，不得因组件重挂载序号归 1 而与不同 selected values 碰撞 [progressive-setup-guide.tsx:46]
- [x] [Review][Patch] Setup 未提交选择点击返回设置前须提示，不得静默丢弃 draft [progressive-setup-guide.tsx:188]
- [x] [Review][Patch] Playwright 持久化 mock 须根据真实 save request 更新后续 query，不得在测试中手工注入预期结果；其余 UI 矩阵缺口在第四组证据审查收口 [story-2-1-configuration-validation.spec.ts:125]

#### 第三组审查中已驳回的非缺陷

- [x] [Review][Dismissed] 当前 `/rules` 生产页面没有调用 React Router 程序化 `navigate`；应用 Link、back/forward 和窗口关闭会直接受本轮 guard 修正覆盖，不为尚不存在的导航路径更换整个 Router 架构。
- [x] [Review][Dismissed] 用户已明确缩减盲人/读屏运行验收；Setup 步骤切换 live-region 广播不属于当前 Windows 最小闭环，保留基本键盘和可见状态即可。

#### 第四组：测试、原生运行证据与最终门禁

- [x] [Review][Patch] Playwright 外联审计须跨 reload 持久，不得清空保存阶段产生的 fetch/XHR/WebSocket/Beacon/Notification 记录。
- [x] [Review][Patch] native configuration smoke 必须自行构建或显式复用已核验候选，验证 blocking 与风险确认前均未落盘，并用真实键盘验证首错和返回焦点。
- [x] [Review][Patch] cold-start 与 configuration smoke 只能清理候选拥有的进程树，关闭期间持续发现后代且所有等待有界；环境变量必须在嵌套 finally 中恢复。
- [x] [Review][Patch] 正式采样前后必须核对 candidate/source fingerprints；原始 30 次样本、native 结果与全量 gate 必须持久化到 `_agentic-out/tests/evidence`，不能只留在 ignored target。
- [x] [Review][Patch] 最终 native 与 30 次 cold-start 必须绑定同一 release candidate；最终候选 SHA-256 为 `f1792aa6d4fc7d26cacf44a9299a2ae9e0698578b034fd821748a426d48a976e`。
- [x] [Review][Patch] 真实 Tauri smoke 暴露风险 Dialog 返回焦点早于 inert 解除；改为 React 状态提交后恢复焦点并重新通过原生验证。

## Dev Notes

### 强制数据流

```text
contracts/fixtures/configuration-validation
→ radar-core normalize/validate/risk/receipt
→ single SQLite owner + immutable configuration version
→ configuration/validate/save Application API
→ exact async Tauri commands
→ DesktopApi fail-closed guards
→ TanStack Query + local form draft
→ /rules + RuleImpactPreview
```

- Rust core 是默认配置、规范化、错误/风险分类、validator version、hash、receipt、revision 和持久化版本的唯一事实源。
- 前端即时提示只能改善输入体验，不能替代保存前 core 验证；React 不得自行判断“高可信”“全部来源停用”或表达式语义。
- 保存配置属于规则生效，不得乐观提交。UI 只有收到权威保存响应后才能展示新版本；保存失败不更新 query cache。
- 当前 source preference 仅是后续同步的配置输入。保存层只接受合同声明的公开来源标识及 `http`/`https`（产品优先提示 HTTPS），拒绝 `file:`、shell、路径和任意协议；本 Story 不探测 URL、不解析 RSS、不访问 GitHub/arXiv、不做 DNS/SSRF/TLS/重定向检查，真实网络安全策略属于 Story 2.2–2.4。

### 与 Story 1.8 的兼容边界

- 直接复用现有 `settings_metadata`、setup progress、`DemoStore`、SQLite v3、async Tauri blocking executor、DesktopApi timeout/error、集中 query keys、`/settings`、`/settings/setup` 和稳定 Router/QueryClient。
- Story 1.8 的 `track_ids`、`source_example_ids`、`refresh_cadence` 已是有效 core-owned 默认值；迁移后它们必须映射到 Story 2.1 的当前配置，不能留两套互相矛盾的事实。
- SetupProgress 仍只保存引导进度；业务配置进入 Story 2.1 的权威版本。跳过/稍后继续不能删除规则配置。
- 延续审查经验：超时重试复用幂等 key；exact revision；真实 UTC 时间；checked arithmetic；幂等表有界；fresh/current schema 启动即验证；真实 reopen/并发/回滚/迟到响应/焦点恢复测试。

### 技术与依赖

- 保持锁定：Rust 1.97.1/edition 2024、rusqlite 0.40.1 bundled、SQLite 3.53.4、serde 1.0.229、serde_json 1.0.151、Tauri core 2.11.5、CLI 2.11.4、React 19.2.8、TypeScript 5.9.3、Vite 8.2.1、TanStack Query 5.101.4、React Router 8.3.0、Tailwind 4.3.3、Vitest 4.1.10、Playwright 1.62.1。
- 不以“latest”升级任何版本。允许为 receipt/hash 在项目 Cargo.toml 增加精确锁定的直接依赖并更新项目 Cargo.lock；不得全局安装，也不得使用时间、PID、原子计数、FNV 或 DefaultHasher 冒充安全 entropy/hash。
- Tauri 官方说明重工作应使用 async command，当前实现继续把 SQLite 阻塞工作放到受控执行器，不能在主线程直接执行。
- TanStack Query mutation 成功后以权威返回值更新或失效关联 query；配置保存禁止 optimistic update。
- SQLite 同时仅允许一个 write transaction，保存使用显式事务并把 revision 检查、版本追加、current 切换、幂等响应和 receipt 消费作为一个应用级原子结果处理。
- 所有依赖、缓存、Rust/Node/Python 工具必须位于 `.toolchains/`、项目 store/cache 或项目 `node_modules`，不得影响全局环境。

### 文件结构要求

优先新增/修改：

```text
contracts/fixtures/configuration-validation/{blocking,narrowing,valid}/
contracts/fixtures/golden/configuration_*.json
contracts/schemas/contract-manifest-v1.json
contracts/snapshots/error-codes-v1.json
crates/radar-core/src/domain/rules/configuration_validation.rs
crates/radar-core/src/application/commands/{validate_configuration.rs,save_configuration.rs}
crates/radar-core/src/application/queries/get_configuration.rs
crates/radar-core/src/infrastructure/database/configuration_repository.rs
crates/radar-core/src/contracts/dto/configuration_validation.rs
crates/radar-core/src/application/{setup.rs,demo.rs}
crates/radar-core/tests/configuration_validation.rs
apps/windows/src-tauri/src/commands/mod.rs
apps/windows/src/lib/desktop-api/{desktop-api,tauri-desktop-api}.ts
apps/windows/src/lib/query-client.ts
apps/windows/src/app/router/app-router.tsx
apps/windows/src/features/configuration-validation/
tests/e2e/story-2-1-configuration-validation.spec.ts
scripts/windows-configuration-smoke.ps1
```

当前仓库还没有完整 `domain/` 与 `infrastructure/` 子树；本 Story 只创建上面列出的最小 rules/repository/commands/queries/DTO 路径，不为未来 Story 预建空模块，也不创建 `warning-manager`、`validation-service`、`force-save`、第二 store 或跨平台 UI 包。

### 回归与反模式护栏

- 不修改或删除现有 demo、FTS、setup defaults/progress、路由 fallback、health、AppError、secret、effect ledger 或 release surface 安全门。
- 不新增 HTTP client、网络 capability、AI provider、通知权限、通知 outbox、云账户、云备份、跨设备同步、历史情报重算或自动权重调整。
- 不把完整配置、receipt 或 DTO 持久化到 localStorage/Zustand/URL；不在组件 useEffect 中直接调用 DesktopApi；不在 Tauri/React 执行 SQL。
- 不用无风险配置统一弹确认，不用 destructive 红色表达 narrowing warning，不用空候选集伪造“全部高可信候选被过滤”。
- 不 reset/checkout/覆盖当前脏工作树；Story 1.6–1.8 的实现大多尚未提交，所有既有变化都应视为用户资产。

### 测试与完成门禁

- 自动化必须使用同一组共享 fixtures 验证 core、Tauri/TS 和 UI；浏览器 mock 不能冒充真实 core/native 证据。
- 正式测试不得依赖公网、系统时间等待、随机端口碰撞、全局环境或开发者系统主题/缩放。
- Rust、Vitest、Playwright 和 native evidence 的每项结论须区分 `passed`、`failed`、`not_measured`；不能把静态源码搜索或硬编码布尔值冒充运行证据。
- 仅当 AC1–AC8 的 Windows-first 证据与全量回归通过后，Story 才能进入 review；Apple/Android 继续 Deferred，不计为 Windows 阻塞项。

### Previous Story Intelligence

- Story 1.8 已完成默认配置、SetupProgress、SQLite v3、精确 Tauri commands、DesktopApi guards、稳定 Router、设置入口、焦点恢复和项目隔离 native/30-sample 证据；本 Story 在其上增量实现，不重建壳层。
- 前序审查修复了 effective defaults、skip 返回、超时幂等恢复、总体状态、schema 启动验证、焦点恢复、exact DTO guard、入口 loading/error、真实时间、bounded idempotency、未知路由和 native 证据真实性；Story 2.1 测试必须防止这些问题复发。
- 生产源码或 lockfile 变化会使旧候选 SHA/provenance 失效；若 Story 2.1 生成新的 native 运行证据，必须重建候选并绑定新的 source/candidate hash，不能沿用 Story 1.8 的候选结论。

### Git Intelligence

- 仓库仍只有基线提交 `f73aed0 chore: initialize Windows-first application`；Story 1.6–1.8 的大量实现位于未提交工作树。实现时以当前文件内容为准，不以短 commit 历史推断能力缺失。

### Latest Technical Information

- [Tauri 2 Calling Rust](https://v2.tauri.app/develop/calling-rust/)：command 可返回序列化 DTO/错误；异步 command 用于避免 UI 主线程阻塞。继续复用现有受控 blocking DB executor。
- [TanStack Query v5 mutation invalidation](https://tanstack.com/query/v5/docs/framework/react/guides/invalidations-from-mutations)：成功 mutation 后按领域 query key 失效或更新缓存；本 Story等待权威保存响应，不做 optimistic domain update。
- [SQLite transactions](https://www.sqlite.org/lang_transaction.html)：SQLite 同时只有一个 write transaction；配置保存必须显式事务化并对 busy/conflict 返回稳定错误。
- [SQLite foreign keys](https://www.sqlite.org/foreignkeys.html)：约束在事务/提交边界执行；新表和迁移继续启用并验证 `foreign_keys=ON`。

### References

- [Epic 2 / Story 2.1 / Windows-first 交付规则](../../planning/epics.md)
- [PRD FR4–FR6、FR57、FR62、NFR29–NFR31、NFR46](../../planning/prd.md)
- [Architecture 配置验证、receipt、Application API、SQLite 与前端状态边界](../../planning/architecture.md)
- [DESIGN RuleImpactPreview、表单、状态与 token](../../planning/ux/DESIGN.md)
- [EXPERIENCE 配置校验双通道与本机数据边界](../../planning/ux/EXPERIENCE.md)
- [UX design specification 规则编辑、错误、风险确认和焦点](../../planning/ux-design-specification.md)
- [Story 1.8 implementation/review learnings](./1-8-使用默认赛道并按自己的节奏完成配置引导.md)

## Requirement Change Log

### RCL-001：将 Story 2.1 权威 AC 细化为可实现的配置安全合同

- **Trigger：** 用户在 Story 2.1 创建、完整性复核及 blocker 修正阶段连续确认并批准实施。
- **Classification：** Story Amendment。
- **Previous behavior：** Epic 只定义八项高层 AC，未固定表达式 grammar、数值/时间边界、candidate context、receipt 参数、v4 schema、Setup 并发投影或 Windows 运行证据口径。
- **New behavior：** 固定 Task 1–6 所列 DTO、四类 blocking/两类 narrowing、parser/规范化/SHA-256、CSPRNG 单次 receipt、SQLite v4 不可变版本和幂等、Setup 局部 patch、精确 Tauri/DesktopApi、Windows 规则表单及项目隔离证据；移动端继续 Deferred。
- **Acceptance Criteria affected：** AC1–AC8、Windows-first 验收边界。
- **Tasks affected：** Task 1–Task 6。
- **Upstream artifacts affected：** PRD、Epics、Architecture、UX/DESIGN/EXPERIENCE；其阶段性 Windows-first 范围已通过批准的 Correct Course 保持一致。
- **Tests required：** 四类 blocking、两类 narrowing、valid、receipt 失效矩阵、fresh/v1/v2/v3→v4、revision/idempotency/rollback/reopen、Tauri/TS strict wire、八 UI 状态、Playwright 三通道、native smoke 和 30 次项目隔离候选证据。
- **Approval evidence：** 当前任务线程中的“确认”“批准”“是”以及随后对第一阶段 15 Story 收缩的明确批准。
- **Status：** applied。

### RCL-002：第一组代码审查修正 Core、迁移和证据完整性

- **Trigger：** 2026-08-17 `agentic-code-review` 第一组 Blind/Edge/Acceptance 三层审查发现 16 项可直接修复问题，用户选择 `0` 批量修复。
- **Classification：** Implementation Correction。
- **Previous behavior：** URL 仅前缀校验；时间窗用字符串比较；receipt 未比较 canonical bytes且消费项占容量；熵失败可产生矛盾 DTO；v3→v4 初始化跨事务；并发 revision 可误报 storage；schema 只查列；Setup 重放返回当前状态；运行期配置损坏、字段定位和部分负例证据不足；候选 source SHA 与 `not_measured` 语义不完整。
- **New behavior：** 按 Review Findings 第一组逐项 fail closed，保持既有 AC、固定错误/风险分类和 Windows-first 范围不变。
- **Acceptance Criteria affected：** AC1–AC5、AC7–AC8、Windows-first 验收边界。
- **Tasks affected：** Task 1–Task 4、Task 6。
- **Upstream artifacts affected：** 无；属于不改变产品范围的实现修正。
- **Tests required：** URL/字段/时间/边界、熵失败、canonical identity、receipt capacity、并发 winner/conflict、revision range、迁移原子回滚、schema lookalike、运行期损坏、Setup 历史重放和未知 enum；更新 native/cold-start source fingerprint 后重新生成候选证据。
- **Approval evidence：** 用户在第一组 Review Findings 操作选择中回复 `0`（批量修复全部非争议问题）。
- **Status：** applied。

### RCL-003：第二组代码审查收紧 IPC 与合同门禁

- **Trigger：** 2026-08-17 `agentic-code-review` 第二组 Blind/Edge/Acceptance 审查发现 Tauri 初始化 panic、DesktopApi fail-closed、fixture 行为与 xtask 扫描绕过问题。
- **Classification：** Implementation Correction。
- **Previous behavior：** 首次打开不在 panic containment 内；AppError/配置 guard 只校验部分外形；响应 hash 未重算；configuration golden 在 xtask 内有副本且未执行；某些 TS 扩展名、invoke 别名和子目录 `.env.example` 可绕过。
- **New behavior：** 初始化与调用统一脱敏 containment；AppError/配置/风险 wire 完整校验；DesktopApi 重算 canonical SHA-256；xtask 与 Tauri helper 直接消费共享 fixture 并执行生产路径；扫描面补齐。Demo V1 既有 additive compatibility 保持不变。
- **Acceptance Criteria affected：** AC1–AC5、AC7–AC8。
- **Tasks affected：** Task 1、Task 4、Task 6。
- **Upstream artifacts affected：** 无；不改变产品范围或交互。
- **Tests required：** Tauri 共享 fixture command helper、FFI blocking/risk/receipt、DesktopApi error/config/hash 负例、xtask extensions/invoke/.env mutation、manifest drift 与完整 Vitest/Rust/Clippy。
- **Approval evidence：** 用户在代码审查修复选择中回复 `0`，授权批量修复非争议问题。
- **Status：** applied。

### RCL-004：第三组代码审查修正 Windows 状态机与真实 E2E 持久化

- **Trigger：** 2026-08-17 `agentic-code-review` 第三组 Blind/Edge/Acceptance 审查发现配置页请求竞态、脏草稿、焦点、Setup 导航与 E2E mock 证据问题。
- **Classification：** Implementation Correction。
- **Previous behavior：** 初始读取错误被 loading 遮蔽；重复保存、stale receipt、provider/revision 切换可产生错误重放；来源编辑和空数字会改变用户输入；Setup 可在保存中离开；E2E 通过测试代码手工注入保存结果。
- **New behavior：** 配置编辑 session 与 provider/revision 绑定，保存互斥并按错误类别重新校验或重放，字段路径与用户文案可操作，脏表单/风险层/Setup 导航保留状态；Playwright mock 根据真实请求计算 hash 并持久化后续查询。
- **Acceptance Criteria affected：** AC1–AC5、AC7–AC8。
- **Tasks affected：** Task 3–Task 6。
- **Upstream artifacts affected：** 无；属于既有 Windows-first 合同的实现修正。
- **Tests required：** configuration 初始错误、stale receipt、字段焦点、Setup remount/dirty/pending、73 项 Vitest、21 项 Playwright、TypeScript format/lint/typecheck。
- **Approval evidence：** 用户在第三组 Review Findings 操作选择中回复 `0`，授权批量修复全部非争议问题。
- **Status：** applied。

### RCL-005：第四组代码审查收紧可复核运行证据

- **Trigger：** 2026-08-17 `agentic-code-review` 第四组发现旧候选、reload 外联清零、原生未验证不落盘、进程树竞态及 raw evidence 仅存 ignored target。
- **Classification：** Verification Correction。
- **Previous behavior：** 浏览器 mock 可在 reload 后丢失外联记录；native/cold-start 可绑定不同或过期候选；关闭脚本可能遗漏迟生后代；旧证据不可随仓库复核。
- **New behavior：** 外联记录在独立浏览器测试上下文 localStorage 累积；原生脚本验证 blocking/确认前不落盘与真实键盘焦点；进程关闭前后持续枚举候选后代且等待有界；30 次和 native 原始证据、全量 gate 持久化并绑定同一候选与源码指纹。
- **Acceptance Criteria affected：** AC1–AC8、Windows-first 运行验收。
- **Tasks affected：** Task 6。
- **Upstream artifacts affected：** 无；属于证据真实性修正。
- **Tests required：** 73 Vitest、21 Playwright、95 Rust、format/lint/typecheck/build、Clippy、xtask、native configuration smoke、30 次 cold-start P95。
- **Approval evidence：** 用户此前选择 `0` 批量修复完整审查发现，并已批准 30 次正式样本口径及原生运行。
- **Status：** applied。

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- 2026-08-17：从完整 sprint status 确认无 ready-for-dev Story；经用户批准选择首个非移动 backlog Story 2.1，并将 Epic 2 切换为 in-progress。
- 2026-08-17：并行复核 Epic/PRD、Architecture/当前代码、Story 1.8/UX；补充官方 Tauri、TanStack Query 与 SQLite依据，确定 Windows-first 配置验证切片。
- 2026-08-17：开始 Story 2.1 实施；状态切换为 in-progress，按合同/验证器 → v4 持久化 → IPC → Windows UI → 运行证据顺序执行。

### Completion Notes List

- 2026-08-17：完成权威配置合同、四类阻断/两类风险 fixture、有界表达式 parser、SHA-256 规范 hash、CSPRNG 单次 receipt、SQLite v4 不可变版本、幂等重放及 setup/configuration 同事务并发保护；fresh/v1/v2/v3→v4、损坏启动合同和离线重启均有 Rust 证据。
- 2026-08-17：接通三个精确异步 Tauri commands、DesktopApi fail-closed guards、`/rules` 一级入口及完整赛道/来源/刷新/阈值/免打扰表单；正常直存、完整阻断错误、首错聚焦、过窄确认、脏表单保护和失败同 intent 重试均已覆盖。
- 2026-08-17：代码审查四组修正后，项目隔离门禁重新通过：frozen install、format、lint、typecheck、build、Vitest 73/73、Playwright 21/21、Rust fmt、Clippy、workspace 95/95、xtask contracts 与 MSVC release；详见 `_agentic-out/tests/evidence/story-2-1-quality-gate.json`。
- 2026-08-17：最终候选 SHA-256 `f1792aa6d4fc7d26cacf44a9299a2ae9e0698578b034fd821748a426d48a976e` 同时通过真实 native configuration smoke 与 30/30 冷启动；P50 1601.65ms、P95 1736.94ms、max 6465.48ms、阈值 5000ms，原始样本和源码 fingerprints 已持久化到 `_agentic-out/tests/evidence/`。
- 2026-08-18：测试自动化严格去重后无新增缺口；test-review 99/100（无 P0/P1）；traceability AC1–AC8 全部 FULL，P0/P1/overall 均 100%，确定性 gate PASS。Story 2.1 Windows milestone 完成。
- 2026-08-18：Apple/iPhone/iPad/Android 保持 Deferred/N/A，不宣称跨平台 MVP 完成。未修改系统主题、缩放或权限；所有工具链和运行数据均在项目内隔离。

### File List

- `_agentic-out/implementation/stories/2-1-管理并安全校验当前设备的关注配置.md`
- `_agentic-out/implementation/sprint-status.yaml`
- `Cargo.lock`
- `contracts/fixtures/configuration-validation/blocking/cases-v1.json`
- `contracts/fixtures/configuration-validation/narrowing/cases-v1.json`
- `contracts/fixtures/configuration-validation/valid/basic-v1.json`
- `contracts/fixtures/golden/configuration_validation_v1.json`
- `contracts/fixtures/golden/setup_progress_v1.json`
- `contracts/schemas/contract-manifest-v1.json`
- `contracts/snapshots/error-codes-v1.json`
- `crates/radar-core/Cargo.toml`
- `crates/radar-core/src/application/configuration.rs`
- `crates/radar-core/src/application/demo.rs`
- `crates/radar-core/src/application/mod.rs`
- `crates/radar-core/src/application/setup.rs`
- `crates/radar-core/src/contracts/dto/configuration_validation.rs`
- `crates/radar-core/src/contracts/dto/mod.rs`
- `crates/radar-core/src/contracts/errors.rs`
- `crates/radar-core/src/domain/mod.rs`
- `crates/radar-core/src/domain/rules/mod.rs`
- `crates/radar-core/src/domain/rules/configuration_validation.rs`
- `crates/radar-core/src/lib.rs`
- `crates/radar-core/tests/configuration_validation.rs`
- `crates/radar-core/tests/generated_effect_contract_edges.rs`
- `crates/radar-core/tests/setup_progress.rs`
- `crates/radar-ffi/Cargo.toml`
- `crates/radar-ffi/src/mapping.rs`
- `crates/radar-ffi/tests/boundary.rs`
- `apps/windows/src-tauri/src/commands/mod.rs`
- `apps/windows/src-tauri/src/lib.rs`
- `apps/windows/src-tauri/tests/release_surface.rs`
- `apps/windows/src/app/router/app-router.tsx`
- `apps/windows/src/features/configuration-validation/configuration-editor.tsx`
- `apps/windows/src/features/configuration-validation/configuration-editor.test.tsx`
- `apps/windows/src/components/ui/button.tsx`
- `apps/windows/src/features/settings/settings-root.tsx`
- `apps/windows/src/lib/desktop-api/desktop-api.ts`
- `apps/windows/src/lib/desktop-api/tauri-desktop-api.ts`
- `apps/windows/src/lib/desktop-api/tauri-desktop-api.test.ts`
- `apps/windows/src/lib/query-client.ts`
- `apps/windows/src/styles/globals.css`
- `apps/windows/src/test/contract-fixtures.test.ts`
- `crates/xtask/src/contracts.rs`
- `crates/xtask/tests/generated_contract_gate_negative.rs`
- `tests/e2e/story-2-1-configuration-validation.spec.ts`
- `tests/support/factories/demo-dto.factory.ts`
- `tests/support/fixtures/demo-app.fixture.ts`
- `tests/support/helpers/tauri-command-mock.ts`
- `scripts/windows-configuration-smoke.ps1`
