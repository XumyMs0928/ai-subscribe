---
artifact_kind: story
status: done
delivery_profile: ''
source_story: ''
baseline_commit: NO_VCS
---

# Story 1.1：建立共享核心与版本化契约基线

状态：done

## Story

作为产品验证者，
我希望先建立可测试的共享领域核心与稳定跨平台契约，
以便三个原生平台可以逐一接入同一业务权威而不复制规则。

## 验收标准

### AC1：共享核心工程基线

**Given** 工作区尚未建立正式应用工程  
**When** 按架构初始化 Cargo workspace、`radar-core`、`radar-ffi`、`contracts` 和 `xtask`  
**Then** 共享核心可独立构建并通过健康检查与纯 Rust 契约测试  
**And** 本 Story 只建立最小契约与工程边界，不提前创建未来业务表或平台 UI。（FR1、ARCH-1、ARCH-3）

### AC2：最小版本化跨平台契约

**Given** 已定义最小版本化 DTO、`AppError`、`PlatformEffect` 和 `SecretLeaseInput`  
**When** 分别执行成功、字段验证失败、未知错误、重复 effect 回报和 secret lease 生命周期测试  
**Then** 字段语义、稳定错误码、幂等结果和密钥生命周期符合契约  
**And** Rust panic 不跨越 FFI，ID 不透明，时间使用 RFC3339 UTC，缺失值显式 `optional/null`，明文不进入持久化或日志。（ARCH-11、ARCH-14、ARCH-16）

## Tasks / Subtasks

- [x] 1. 建立最小 Rust workspace 和仓库基线（AC1）
  - [x] 创建根 `Cargo.toml` 虚拟 workspace，成员仅包含 `crates/radar-core`、`crates/radar-ffi`、`crates/xtask`；显式设置 Cargo resolver，并共享 edition、license 和版本元数据。
  - [x] 创建 `rust-toolchain.toml`，精确锁定经本 Story 验证的 Rust stable，并包含 `rustfmt`、`clippy`；提交根 `Cargo.lock`，禁止浮动工具链。
  - [x] 创建 `crates/radar-core`、`crates/radar-ffi`、`crates/xtask` 及 `contracts/{schemas,fixtures/golden,snapshots}` 的最小目录和清晰职责说明。
  - [x] 配置根级 `rustfmt.toml`、`.editorconfig`、`.gitignore` 和最小 README；忽略 `target/`、真实 secret、用户数据、数据库及生成物临时目录。当前项目根不是 Git 仓库，本 Story 不擅自初始化 Git；锁文件须生成并保留在工作区，待仓库初始化后纳入版本控制。
  - [x] 不创建 `apps/windows`、`apps/apple`、`apps/android`、业务数据库、迁移、来源适配器或任何平台 UI。

- [x] 2. 建立共享核心健康检查和单一权威边界（AC1）
  - [x] 在 `radar-core` 定义无平台依赖的最小健康检查用例及版本化返回 DTO，可由纯 Rust 测试直接调用。
  - [x] 保持依赖方向为 `radar-ffi -> radar-core`；`radar-core` 不依赖 UniFFI、React、SwiftUI、Compose、Tauri 或平台 SDK。
  - [x] `radar-ffi` 只提供版本化用例/DTO/错误的薄映射与 panic 防护入口，不承载业务判断，不暴露 SQL、文件、URL、HTTP client 或任意命令能力。
  - [x] `xtask` 提供可扩展的 `contracts` 校验入口，通过 `cargo run -p xtask -- contracts` 调用；成功返回 0，schema/snapshot/fixture 不一致返回非 0。本 Story 不安装全局 `cargo-xtask`，也不承担 Story 1.5 的 Swift/Kotlin 绑定生成与三端编译门禁。

- [x] 3. 定义最小、稳定、版本化合同（AC2）
  - [x] Rust 类型是运行时权威；建立 `contract_version: 1` 的最小 contract manifest，列出所有 public DTO 字段、JSON `snake_case` 名称、枚举全集和兼容性规则（同版本只允许兼容新增；删除字段、改变枚举/错误语义需显式迁移 ADR）。在 `contracts/schemas` 与 `contracts/snapshots` 保存可读镜像和稳定枚举/错误码快照。
  - [x] 定义不透明 ID 新类型；序列化边界不得暴露数据库 rowid、内部整数或可推断结构。
  - [x] 时间字段只接受/输出带时区的 RFC3339 UTC；缺失值使用 `Option<T>` 并在 JSON 镜像中显式表现为 `null`，不得用空字符串、零或 `unknown` 代替缺失。
  - [x] 定义 `AppError`：至少包含 `code`、`category`、`message_key`、`retryability`、`source_id?`、`task_id?`、`details_allowlisted`、`correlation_id`；稳定 category 与 retryability 必须使用架构规定值。
  - [x] 未知底层错误统一映射为 `internal` 并生成 correlation ID；底层错误文本不得进入面向平台的 DTO。
  - [x] 定义 `PlatformEffect`：至少包含稳定 effect ID、幂等键、effect 类型、payload version、过期时间和执行状态；contract manifest 必须列出初始/终态、合法转换矩阵、相同重复回报的固定幂等结果，以及不同终态重复回报产生的固定 `conflict` `AppError`。最小 effect 类型可使用不执行真实副作用的合同测试类型，不提前实现通知等业务 effect。
  - [x] 定义短生命周期 `SecretLeaseInput`，禁止派生或实现会输出明文的 `Debug`/`Display`/序列化行为；明文不得成为 task payload、持久 DTO 或日志字段。

- [x] 4. 建立纯 Rust 合同测试与固定样本（AC1、AC2）
  - [x] 健康检查成功：验证独立构建、合同版本和确定性返回。
  - [x] 字段校验失败：覆盖非法/空 ID、非法 RFC3339 UTC、未知受限枚举、超限字段及不合法状态转换，并断言稳定 `validation` 错误码。
  - [x] 未知错误与 panic：通过 `radar-ffi` 的纯 Rust 可测试适配入口和 `catch_unwind` 验证二者被转换为 `internal` `AppError`，panic 不逃逸该适配边界，错误中含 correlation ID 且不泄露底层文本；不得声称已完成 Swift/Kotlin 绑定往返。
  - [x] effect 幂等：同一 effect ID/幂等键首次回报生效，完全相同的重复回报返回稳定幂等结果且不产生第二次状态变化；冲突回报返回稳定 `conflict`。
  - [x] secret lease 生命周期：golden fixture 只保存不含明文的 `secret_ref`、场景和预期元数据；运行时由进程内 fake provider 生成 canary 字节并注入一次受控调用，调用后不可重用。`SecretLeaseInput` 不实现会输出明文的 `Debug`、`Display` 或 serde 序列化；扫描捕获日志、JSON、snapshot、错误文本及测试输出并断言 canary 零命中，失败信息也不得回显原字节。
  - [x] 为成功、校验失败、未知错误、effect 回报和 secret lease 建立唯一的 `contracts/fixtures/golden/{health_success_v1,validation_failure_v1,internal_error_v1,effect_report_v1,secret_lease_v1}` 样本；每个样本在 manifest 标明输入、期望输出和禁止字段，禁止在 crate 或未来平台目录复制 fixture。
  - [x] 增加 schema-conformance 测试：枚举所有 public DTO 并校验 `contract_version`、字段名、显式 `null`、枚举全集、`AppError` category/retryability 和 error-code snapshot，而非只抽测单一样例。

- [x] 5. 建立本 Story 的纯 Rust 基线门禁与交接证据（AC1、AC2）
  - [x] 运行 `cargo fmt --all --check`、`cargo clippy --workspace --all-targets --all-features -- -D warnings`、`cargo test --workspace --all-targets` 和 `cargo run -p xtask -- contracts`。
  - [x] 验证工作区可在无注册、无云账户、无 AI Key、无平台 SDK 和无数据库的环境中完成 core 构建与测试。
  - [x] 在当前非 Git 工作区执行确定性的目录/内容扫描：允许 `contracts/schemas/**`，但禁止 `apps/**`、`migrations/**`、SQL DDL、`rusqlite` 依赖、真实 secret、Authorization header、数据库文件和未来业务表名；同时扫描测试输出和捕获日志。该边界检查应纳入 `xtask contracts` 或独立 `xtask boundaries` 子命令。
  - [x] 在本 Story 的 Dev Agent Record 中记录命令、结果、锁定版本和文件清单，为 Stories 1.2–1.5 提供可复用基线。

### Review Findings

- [x] [Review][Patch] 让 contract manifest/error snapshot 由独立 Rust 权威生成，消除同源自比较假门禁 [`crates/radar-core/src/contracts/manifest.rs`:3]
- [x] [Review][Patch] 对五个 golden fixture 执行结构与行为语义校验，而非只搜三个禁止词 [`crates/xtask/src/contracts.rs`:51]
- [x] [Review][Patch] 收紧 SecretLease 单次操作 API，并确保构造失败路径也清零敏感字节 [`crates/radar-core/src/contracts/secrets.rs`:18]
- [x] [Review][Patch] 封装 PlatformEffect 状态字段并实现基于 effect ID/idempotency key 的内存幂等账本 [`crates/radar-core/src/contracts/effects.rs`:48]
- [x] [Review][Patch] 封装 AppError、使用受控错误码并为内部错误生成唯一 correlation ID [`crates/radar-core/src/contracts/errors.rs`:80]
- [x] [Review][Patch] 补齐 radar-ffi 的批准用例和 DTO/wire 映射，使健康检查与错误映射经过真实适配入口 [`crates/radar-ffi/src/api.rs`:1]
- [x] [Review][Patch] 增加可执行 wire JSON 证据，覆盖字段名、显式 null 与稳定枚举字符串 [`crates/radar-ffi/src/mapping.rs`:1]
- [x] [Review][Patch] 修复边界扫描的目录链接越界/循环风险并在遍历时剪枝忽略目录 [`crates/xtask/src/contracts.rs`:120]
- [x] [Review][Patch] 扩大边界扫描的受检文本类型与敏感字段规则，校准完成声明 [`crates/xtask/src/contracts.rs`:91]
- [x] [Review][Patch] 补充项目隔离工具链的干净检出安装/校验说明 [`README.md`:5]
- [x] [Review][Patch] 为本次实现行为补齐 AC→Task→Test→Requirement Change Log 追踪链 [本文件:Requirement Change Log]

## Dev Notes

### 权威范围与拆分决策

- 最新 Epics 与已批准的 Correct Course 是 Story 边界权威。架构文档中“首个实现 Story 同时建立三端壳、生成绑定并完成三端编译”的文字属于拆分前聚合方案，已被 2026-08-13 的 Story 1.1–1.5 拆分取代。
- 本 Story 只交付 Cargo workspace、`radar-core`、`radar-ffi`、`contracts`、`xtask`、最小合同和纯 Rust 测试。
- Windows/Tauri/React 壳与 `DesktopApi` 属于 Story 1.2；SwiftUI 与 `RadarCoreClient` actor 属于 1.3；Compose 与 repository/ViewModel/StateFlow 属于 1.4；生成绑定、三端合同往返、三端编译和完整 CI 门禁属于 1.5。
- 演示数据属于 1.6；原生列表/详情、视觉令牌落地和辅助技术验收属于 1.7；默认赛道与配置引导属于 1.8。
- 不得借“预留”之名创建空平台壳、未来业务表、通用 `utils/helpers/services`、云 API、账户体系、SQLCipher、自动更新、公共商店、远程诊断或云同步。

### 架构约束

- `radar-core` 是确定性规则、未来数据库/迁移/任务和跨平台运行时合同的唯一权威。平台不得复制领域规则；`radar-ffi` 只做稳定类型和错误映射。
- Application API 使用类型化 DTO 和 Rust 结果类型，不增加 `{data,error}` 包装。Rust/JSON 使用 `snake_case`；未来 Swift/Kotlin/TypeScript wrapper 可映射为惯用 `camelCase`，但不能改变语义。
- `AppError.category` 固定集合：`validation`、`not_found`、`conflict`、`permission`、`network`、`rate_limited`、`source_format`、`provider`、`storage`、`migration`、`cancelled`、`internal`。
- `AppError.retryability` 固定集合：`never`、`manual`、`automatic`、`after`。`message_key` 用于未来平台本地化，不能返回拼接的用户长文。
- `PlatformEffect` 的最终架构是持久 outbox，但本 Story 禁止提前创建数据库表；只建立纯领域合同、状态转换和内存 fake，以便后续持久化实现复用同一语义。
- `SecretLeaseInput` 只是一次执行会话的短生命周期输入。本 Story 用 fake provider 验证合同，不接入 Windows Credential Manager、Keychain 或 Android Keystore。
- FFI 输入一律视为不可信；输入长度、枚举和嵌套深度应有显式边界。不得跨 FFI 暴露数据库连接、Future、Stream、长生命周期 callback 或可变共享对象。

### 技术版本与最新官方核验

- 架构锁定 UniFFI `0.32.0`。该版本包含破坏性变化：旧式 `--config` 覆盖文件会被忽略、`[ByRef] bytes` 在 Rust 侧映射为 `&[u8]`、Kotlin/Python 对 async primary constructor 直接拒绝生成。本 Story 不使用 async constructor、旧式 config 或实验性 Kotlin JNI 管线。
- UniFFI 官方建议在多 crate workspace 尽可能使用 library mode 生成绑定；实际 Swift/Kotlin 生成与漂移校验归 Story 1.5。本 Story 仅保证 `radar-ffi` 的合同可由该模式消费。
- UniFFI 对 Swift 6 的支持仍有边界，尤其 async/`Sendable`；因此合同保持同步、窄 DTO、显式错误，不在边界暴露复杂异步对象。
- 创建 Story 时官方最新 Rust stable 为 `1.97.1`。实现时应将实际验证通过的稳定版本精确写入 `rust-toolchain.toml`，而不是只写浮动 `stable`；若选择不同版本，须记录验证依据，不得静默漂移。
- 根虚拟 Cargo workspace 应显式指定 resolver；Rust 2024 workspace 可使用 `resolver = "3"`。所有 workspace package 共享根 `Cargo.lock`。

### 项目结构要求

```text
Cargo.toml
Cargo.lock
rust-toolchain.toml
rustfmt.toml
.editorconfig
.gitignore
README.md
crates/
  radar-core/
    Cargo.toml
    src/
      lib.rs
      application/
      contracts/
        dto/
        errors.rs
        effects.rs
        secrets.rs
    tests/
      contracts/
  radar-ffi/
    Cargo.toml
    src/
      lib.rs
      api.rs
      mapping.rs
      error.rs
    tests/
  xtask/
    Cargo.toml
    src/
      main.rs
      contracts.rs
contracts/
  schemas/
  fixtures/
    golden/
  snapshots/
```

- 这是 Story 1.1 的允许上界，不要求为了匹配完整架构树而预建空业务模块。
- `contracts/` 是跨平台 fixture 与可读镜像的唯一位置；生成内容必须标注生成器版本和“不可手改”。
- 测试应与模块镜像；跨模块合同测试放在 crate `tests/`，单元测试与实现共置。

### 测试与完成判据

- Test Design 产物当前缺失；standard profile 允许继续，但本 Story 必须把 AC2 的五类合同测试作为阻断性完成条件，不得以“后续测试”替代。
- 测试必须断言语义和稳定输出，不只断言函数未报错。golden snapshot 的变化必须能区分兼容新增和破坏性修改。
- NFR15 是派生安全约束：完整密钥不得命中数据库、持久 UI 状态、日志、诊断或测试数据。平台安全存储的端到端验收归 Stories 1.2–1.4/5.1，本 Story 只验证 core 合同和输出无明文。
- Story 完成前不得声称三端可构建、UniFFI 绑定无漂移、语言侧合同往返或平台密钥安全存储已验证；这些证据分别由 1.2–1.5 提供。

### UX 兼容边界

- 本 Story 不创建 UI、导航、主题或设计 token 实现。只需保证合同不会迫使未来三端共享 DOM/UI，且不会阻碍平台原生状态、无障碍和本地化映射。
- UX 的视觉权威是 `ux/DESIGN.md`，行为/状态权威是 `ux/EXPERIENCE.md`；从 Story 1.2 起按平台原生方式消费，不得在 1.1 创建伪共享 UI 层。

### Planning Gate

- 解析 profile：`standard`；来源：schema-v2 compatibility fallback。
- 风险建议：`assured`，原因是基础合同跨越多个权威产物并影响重要 NFR；未显式覆盖当前 profile。
- PRD、UX、Architecture、Epics、Readiness 均为 complete；Readiness 结论为 READY。
- Test Design 缺失。用户已指示继续，视为接受 standard profile 下的该项风险；上述合同测试仍为 Story 内强制项。

### References

- [Source: `_agentic-out/planning/epics.md`，Story 1.1 与 Stories 1.2–1.8，L395–591]
- [Source: `_agentic-out/planning/epics.md`，ARCH-1–ARCH-16，L149–166]
- [Source: `_agentic-out/planning/sprint-change-proposal-2026-08-13.md`，Story 1.1 split，L75–109]
- [Source: `_agentic-out/planning/prd.md`，FR1，L522–529；NFR15，安全与隐私]
- [Source: `_agentic-out/planning/architecture.md`，起始模板评估，L114–236]
- [Source: `_agentic-out/planning/architecture.md`，密钥与 FFI 边界，L381–425]
- [Source: `_agentic-out/planning/architecture.md`，API、PlatformEffect、AppError、合同治理，L521–672]
- [Source: `_agentic-out/planning/architecture.md`，一致性规则与项目结构，L1089–1202、L1298–1505]
- [Source: `_agentic-out/planning/ux/DESIGN.md`，实现权威与平台映射]
- [Source: `_agentic-out/planning/ux/EXPERIENCE.md`，行为与状态权威]
- [UniFFI 0.32.0 changelog](https://github.com/mozilla/uniffi-rs/blob/main/CHANGELOG.md#v0320-backend-crates-v0320---2026-06-30)
- [UniFFI foreign-language binding generation](https://mozilla.github.io/uniffi-rs/latest/tutorial/foreign_language_bindings.html)
- [UniFFI Swift binding and Swift 6 notes](https://mozilla.github.io/uniffi-rs/latest/swift/overview.html)
- [Cargo workspaces](https://doc.rust-lang.org/stable/cargo/reference/workspaces.html)
- [rust-toolchain.toml](https://rust-lang.github.io/rustup/overrides.html#the-toolchain-file)
- [Rust official release announcements](https://blog.rust-lang.org/releases/)

## Dev Agent Record

### Agent Model Used

OpenAI Codex（GPT-5）

### Implementation Plan

- 按红—绿—重构建立虚拟 Cargo workspace；先以缺失合同函数验证测试确实失败，再实现最小合同。
- 保持 `radar-core` 为运行时权威、`radar-ffi` 为纯 Rust 薄映射与 panic containment、`xtask` 为可复跑合同/边界门禁。
- 使用项目隔离 Rust 1.97.1 与便携 LLVM-MinGW，不修改用户或系统 PATH，不引入第三方 crate。
- 通过固定 v1 manifest、error snapshot、五类 golden fixtures 和测试覆盖 AC1/AC2；不创建平台壳、数据库或后续业务能力。

### Debug Log References

- 2026-08-13：REQUIREMENT_CHANGE_GATE 分类为按既有 Story 实施；无 Story Amendment 或 Cross-artifact Change。
- 2026-08-13：开发前置检查确认 Readiness 完成、Test Design 缺失（standard profile 警告）；本机未安装 `rustc`、`cargo` 或 `rustup`，等待安装工具链后执行红—绿—重构。
- 2026-08-13：用户要求改用项目隔离工具链；分类为 Implementation Correction。Rust 将仅安装到 `.toolchains/{cargo,rustup}`，不修改用户/系统 PATH。
- 2026-08-13 RED：首次测试因缺少合同实现失败；项目隔离 MSVC 目标又暴露缺失系统 `link.exe`。改用项目内官方 `x86_64-pc-windows-gnullvm` 标准库与便携 LLVM-MinGW 22.1.8，不安装机器级 Visual Studio Build Tools。
- 2026-08-13 GREEN：实现健康 DTO、OpaqueId、AppError、PlatformEffect 幂等状态机、SecretLeaseInput 单次使用与 RAII 清零、FFI panic/unknown error 映射、v1 mirrors/fixtures 和 xtask 门禁。
- 2026-08-13 REFACTOR/VERIFY：`fmt --check`、Clippy `-D warnings`、9 个合同/FFI 测试和 `xtask contracts` 全部通过。
- 2026-08-13 CODE REVIEW：三层对抗审查完成；合并并修复 11 项，包含合同门禁、secret 生命周期、effect 幂等、错误关联、FFI wire 映射和边界扫描；8 条 Requirement Change Log 已补齐。
- 2026-08-13 AC2.10 REVIEW：三路审查发现 `cfg(test)` observer 不能独立保证 release 清零、失败诊断可能回显 canary、捕获测试未走 lease 数据流；已改用 `zeroize 1.9.0` 全容量清零，将 runtime canary 经 `SecretLeaseInput::with_secret` 注入 panic，并在任何失败输出前先扫描 canary。
- 2026-08-13 AC2.10 VERIFY：项目隔离工具链下 31/31 测试、fmt、Clippy、contract gate 全部通过，5/5 burn-in 稳定。
- 2026-08-13 AC2.10 RED/GREEN：补充真实子进程 stdout/stderr canary 捕获（零命中）与仅在 `radar-core` 单元测试编译中启用的 drop 后清零观察器；成功、业务错误、panic 和构造校验失败路径均直接观察到全零字节，生产 API 与生产构建布局不增加审计入口。
- 2026-08-13 AC1.2/AC2.8 RED/GREEN：新增越界 surface mutation 用例首次暴露 `.sql` 未扫描，随后修复文本类型、空白归一化和 extensionless 文本扫描；RFC3339 UTC 补齐大小写、`+00:00`、四位年份、实际已公告闰秒与 canonical 长度证据。
- 2026-08-13 FINAL VERIFY：项目隔离工具链下 32/32 测试、fmt、Clippy、contract gate 与 5/5 burn-in 通过；traceability 为 12/12 FULL，确定性质量门 PASS。

### Completion Notes List

- 建立仅含 `radar-core`、`radar-ffi`、`xtask` 的 Rust 2024 workspace，锁定 Rust 1.97.1 和 `Cargo.lock`；无第三方 crate。
- v1 合同覆盖稳定错误类别/重试语义、不透明 ID、严格 RFC3339 UTC、显式 null、effect 合法转换/幂等/冲突和一次性 secret lease。
- Secret 明文仅由进程内 fake 在同步闭包中访问，消费后 RAII 清零；合同镜像、fixture、错误和可观察输出不含 canary。
- `radar-ffi` 的纯 Rust 边界捕获 panic、屏蔽私有错误文本；未生成 Swift/Kotlin 绑定，也未声称三端编译完成。
- `xtask contracts` 校验 schema/snapshot/五类 fixture，并拒绝越界的 apps、migrations、数据库/SQL、secret 和高权限接口模式。
- 项目隔离工具链位于 `.toolchains/` 并被忽略；`scripts/rust-env.cmd` 临时设置进程环境，可在不修改全局 PATH 的情况下复跑全部门禁。

### Change Log

- 2026-08-13：完成 Story 1.1 共享核心与版本化契约基线，状态转为 `review`。
- 2026-08-13：完成代码审查批量修复；因 standard profile 的 automation 产物尚未登记，保持 `review`，不提升为 `done`。
- 2026-08-13：补齐 AC1.2、AC2.8，完整质量门与 traceability 均通过；Story 状态转为 `done`。

### File List

- `.cargo/config.toml`
- `.editorconfig`
- `.gitignore`
- `Cargo.lock`
- `Cargo.toml`
- `README.md`
- `rust-toolchain.toml`
- `rustfmt.toml`
- `scripts/rust-env.cmd`
- `contracts/schemas/contract-manifest-v1.json`
- `contracts/snapshots/error-codes-v1.json`
- `contracts/fixtures/golden/health_success_v1.json`
- `contracts/fixtures/golden/validation_failure_v1.json`
- `contracts/fixtures/golden/internal_error_v1.json`
- `contracts/fixtures/golden/effect_report_v1.json`
- `contracts/fixtures/golden/secret_lease_v1.json`
- `crates/radar-core/Cargo.toml`
- `crates/radar-core/src/lib.rs`
- `crates/radar-core/src/application/mod.rs`
- `crates/radar-core/src/application/health.rs`
- `crates/radar-core/src/contracts/mod.rs`
- `crates/radar-core/src/contracts/dto/mod.rs`
- `crates/radar-core/src/contracts/errors.rs`
- `crates/radar-core/src/contracts/effects.rs`
- `crates/radar-core/src/contracts/manifest.rs`
- `crates/radar-core/src/contracts/secrets.rs`
- `crates/radar-core/tests/contracts.rs`
- `crates/radar-core/tests/contracts/health_contract.rs`
- `crates/radar-core/tests/contracts/contract_behaviors.rs`
- `crates/radar-core/tests/generated_secret_lease_negative.rs`
- `crates/radar-ffi/Cargo.toml`
- `crates/radar-ffi/src/lib.rs`
- `crates/radar-ffi/src/api.rs`
- `crates/radar-ffi/src/error.rs`
- `crates/radar-ffi/src/mapping.rs`
- `crates/radar-ffi/tests/boundary.rs`
- `crates/radar-ffi/tests/secret_observability.rs`
- `crates/xtask/Cargo.toml`
- `crates/xtask/src/main.rs`
- `crates/xtask/src/contracts.rs`
- `_agentic-out/implementation/stories/1-1-建立共享核心与版本化契约基线.md`
- `_agentic-out/implementation/sprint-status.yaml`
- `_agentic-out/artifacts.yaml`
- `.toolchains/`（本地忽略的项目隔离 Rust/LLVM-MinGW 运行环境）

## Requirement Change Log

<!-- 追加式记录。每个行为/范围变更必须包含：Trigger、Classification、Previous behavior、New behavior、Acceptance Criteria affected、Tasks affected、Upstream artifacts affected、Tests required、Approval evidence、Status。 -->

### RCL-001：项目隔离 Rust/LLVM 工具链

- Trigger: 用户要求把 Rust 1.97.1、rustfmt、Clippy 改为项目隔离安装。
- Classification: implementation/environment clarification.
- Previous behavior: 工具链安装位置未固定，可能误用全局环境。
- New behavior: `.toolchains/` 持有 Cargo/rustup/LLVM-MinGW，`rust-env.cmd` 只为子进程设置环境且不回退全局 Cargo。
- Acceptance Criteria affected: AC1.
- Tasks affected: 1.2、1.4、5.1、5.2.
- Upstream artifacts affected: none; Story 范围不变。
- Tests required: `rust-env.cmd --version`、fmt、clippy、workspace tests、xtask contracts。
- Approval evidence: 用户明确指示“改成项目隔离安装”“继续”。
- Status: applied; reviewed.

### RCL-002：GNU LLVM Windows 目标替代不可用 MSVC 链接器

- Trigger: 项目隔离环境没有 Visual Studio/MSVC linker，纯 Rust 基线必须可独立复跑。
- Classification: implementation correction, no product behavior change.
- Previous behavior: 默认 Windows MSVC target 依赖全局平台 SDK/linker。
- New behavior: 固定 `x86_64-pc-windows-gnullvm` 并由项目内 LLVM-MinGW 链接。
- Acceptance Criteria affected: AC1.
- Tasks affected: 1.2、5.1、5.2.
- Upstream artifacts affected: none; 三端构建仍归 Story 1.5。
- Tests required: workspace build/test and clippy on the locked target。
- Approval evidence: 用户批准项目隔离安装并要求继续实现。
- Status: applied; reviewed.

### RCL-003：版本化健康与 wire DTO 基线

- Trigger: AC1/AC2 要求共享核心健康检查、版本化 DTO 和显式 null 语义。
- Classification: planned behavior implementation.
- Previous behavior: 无正式 core/FFI 健康合同。
- New behavior: `health_check` 产生确定性 v1 DTO；`radar-ffi::api::health_v1` 经 panic guard 映射并输出明确 JSON null。
- Acceptance Criteria affected: AC1、AC2.
- Tasks affected: 2.1、2.3、4.1、4.7.
- Upstream artifacts affected: epics/architecture intent implemented without scope change.
- Tests required: health contract、approved adapter JSON mapping test。
- Approval evidence: Story 进入开发并由用户要求继续代码审查修复。
- Status: applied; reviewed.

### RCL-004：受控 AppError 与内部错误关联标识

- Trigger: AC2 要求稳定错误码、未知错误脱敏和可关联错误；审查发现任意错误码及固定 correlation ID。
- Classification: planned behavior plus review correction.
- Previous behavior: 公共字段/任意静态错误码，panic 与 unknown error 各复用固定 correlation ID。
- New behavior: `ErrorCode` 是唯一 v1 allowlist，字段只读；每次内部错误生成不同的非敏感 correlation ID。
- Acceptance Criteria affected: AC2.
- Tasks affected: 3.4、3.5、4.2、4.3、4.7.
- Upstream artifacts affected: none; error snapshot 同步实现。
- Tests required: error enum snapshot、panic/unknown mapping、correlation uniqueness、wire mapping。
- Approval evidence: 用户要求修复代码审查发现。
- Status: applied; reviewed.

### RCL-005：PlatformEffect 状态机与内存幂等账本

- Trigger: AC2/ARCH-16 要求重复回报幂等；审查发现公共状态可绕过且 key 未参与判定。
- Classification: planned behavior plus review correction.
- Previous behavior: 单个可克隆对象自行变更公开 status，只提供对象内重复判断。
- New behavior: effect 字段只读且不可克隆；`EffectLedger` 以 idempotency key/identity 统一注册和回报，重复/冲突结果稳定。
- Acceptance Criteria affected: AC2.
- Tasks affected: 3.6、4.4、4.7.
- Upstream artifacts affected: none; 持久 outbox 仍为后续 Story。
- Tests required: first/repeat/conflict、duplicate-key identity、invalid transition。
- Approval evidence: 用户要求修复代码审查发现。
- Status: applied; reviewed.

### RCL-006：SecretLeaseInput 失败路径与单次消费

- Trigger: AC2/NFR15 派生约束；审查发现回调可直接返回 owned secret 且构造失败未先进入清零包装。
- Classification: security correction.
- Previous behavior: 泛型回调可把明文作为返回值带出；非法 secret_ref 路径直接释放原 Vec。
- New behavior: 敏感字节进入函数后立即由清零包装拥有；回调只返回 `Result<(), AppError>`，消费后不可复用，reference 使用受控字符集。
- Acceptance Criteria affected: AC2.
- Tasks affected: 3.7、4.5、5.3.
- Upstream artifacts affected: none; 平台安全存储仍归 Stories 1.2–1.4/5.1。
- Tests required: runtime canary first-use/second-use、invalid-reference、可观察产物零命中。
- Approval evidence: 用户要求修复代码审查发现。
- Status: applied; reviewed.

### RCL-007：独立合同生成与可执行 golden fixtures

- Trigger: Tasks 3/4/5 要求防漂移；审查发现 schema/snapshot 同源自比且 fixture 只检查文件存在。
- Classification: verification correction.
- Previous behavior: `include_str!` 同一磁盘文件作为 expected；fixture 不执行输入/预期行为。
- New behavior: Rust 常量/枚举独立生成 manifest/error snapshot；xtask 紧凑化后比较镜像，并执行五个 fixture 对应行为。
- Acceptance Criteria affected: AC1、AC2.
- Tasks affected: 2.4、3.1、4.6、4.7、5.1.
- Upstream artifacts affected: none; 合同语义仅被明确化。
- Tests required: xtask contracts plus mutation-sensitive mirror/fixture comparisons。
- Approval evidence: 用户要求修复代码审查发现。
- Status: applied; reviewed.

### RCL-008：工程边界扫描加固

- Trigger: AC1 范围限制与 Task 5.3；审查发现遍历未剪枝且可能跟随目录链接越界。
- Classification: verification/security correction.
- Previous behavior: 先遍历被忽略目录后再跳过文件，链接可能越界/循环，文本类型与敏感名覆盖不足。
- New behavior: 遍历前剪枝，拒绝链接并跟踪 canonical 目录；扩展受检文本/敏感文件名和模式。
- Acceptance Criteria affected: AC1、AC2.
- Tasks affected: 1.5、5.2、5.3.
- Upstream artifacts affected: none.
- Tests required: xtask boundaries/contracts and clean workspace scan。
- Approval evidence: 用户要求修复代码审查发现。
- Status: applied; reviewed.

### RCL-009：AC2.10 可观察输出与内存清零的独立证据

- Trigger: traceability 门禁判定 AC2.10 仅部分覆盖；用户要求优先补齐并重新运行完整测试与追踪。
- Classification: implementation correction；既有 AC2/NFR15 行为与范围不变，仅补强可审计验证证据。
- Previous behavior: 对象级断言证明错误 DTO 不含明文、租约不可复用，并由 `SecretBytes::drop` 执行清零；但未捕获真实测试子进程 stdout/stderr，也无法在清零执行后独立观察字节状态。
- New behavior: FFI 集成测试将 runtime canary 经 `SecretLeaseInput::with_secret` 注入受控 panic，启动当前测试二进制子进程并捕获真实 stdout/stderr，在报告失败前先验证 canary 零命中；`SecretBytes` 使用锁定的 `zeroize 1.9.0` 通过 volatile 写与 compiler fence 擦除 Vec 全容量，`cfg(test)` 私有 observer 审计可见字节，且不扩大生产 API、不引入本项目 unsafe。
- Acceptance Criteria affected: AC2（trace item AC2.10）。
- Tasks affected: 4.5、5.1、5.3。
- Upstream artifacts affected: none；平台安全存储与跨进程内存取证仍不属于 Story 1.1。
- Tests required: 成功/业务错误/panic/构造失败的 drop 后全零断言；真实子进程 stdout/stderr canary 零命中断言；随后由主流程执行完整 workspace 测试与 traceability。
- Approval evidence: 用户明确要求“优先补齐 AC2.10，重新运行完整测试和 traceability”。
- Status: applied; reviewed；31/31、fmt、Clippy、contract gate 与 5/5 burn-in 均通过；traceability 已复跑。

### RCL-010：AC1.2 工程边界与 AC2.8 RFC3339 UTC 完整证据

- Trigger: AC2.10 补齐后的 traceability 仍将 AC1.2、AC2.8 判为 PARTIAL；用户要求继续收口。
- Classification: verification and implementation correction；不扩大 Story 范围。
- Previous behavior: workspace scanner 未读取 `.sql`，SQL DDL 可用空白变化或 extensionless 文本绕过；时间测试只证明 canonical 子集。
- New behavior: scanner 覆盖 `.sql` 与 extensionless 文本，对 SQL DDL 做词法空白归一化，并独立测试 apps、migrations、DDL、rusqlite 与敏感文件；RFC3339 UTC 接受 `T/t`、`Z/z`、`+00:00`、fraction、四位年份和已公告闰秒，输出统一 uppercase `T...Z`，拒绝未公告闰秒、非法日期和非 UTC offset。
- Acceptance Criteria affected: AC1、AC2（trace items AC1.2、AC2.8）。
- Tasks affected: 1.5、3.3、4.2、5.1、5.3。
- Upstream artifacts affected: none；manifest/schema 仅明确既有 UTC 输入 profile。
- Tests required: targeted mutation/RFC3339 tests、完整 workspace tests、fmt、Clippy、xtask contracts、5/5 burn-in、traceability。
- Approval evidence: 用户要求继续，并在前序指令中明确要求完整测试与 traceability。
- Status: applied; reviewed；32/32、所有静态门禁与 5/5 burn-in 通过；12/12 FULL，gate PASS。

## Suggested Review Order

**生产清零保证**

- 以 volatile 写和 fence 擦除 Vec 全容量，避免 release 优化移除。
  [`secrets.rs:17`](../../../crates/radar-core/src/contracts/secrets.rs#L17)

- 精确锁定经核验的零化依赖版本。
  [`Cargo.toml:11`](../../../crates/radar-core/Cargo.toml#L11)

**真实输出泄漏证据**

- runtime canary 经 lease 数据流进入受控 panic，再捕获进程输出。
  [`secret_observability.rs:10`](../../../crates/radar-ffi/tests/secret_observability.rs#L10)

- 先扫描 canary 后报告脱敏失败，避免失败路径二次泄漏。
  [`secret_observability.rs:38`](../../../crates/radar-ffi/tests/secret_observability.rs#L38)

**生命周期分支证据**

- 审计成功、错误、panic 与构造失败四条清零路径。
  [`secrets.rs:223`](../../../crates/radar-core/src/contracts/secrets.rs#L223)

**UTC 合同归一化**

- 单一入口规范 UTC profile。
  [`effects.rs:221`](../../../crates/radar-core/src/contracts/effects.rs#L221)

- 边界矩阵验证 canonical 输出。
  [`generated_effect_contract_edges.rs:143`](../../../crates/radar-core/tests/generated_effect_contract_edges.rs#L143)

**工程边界门禁**

- 统一空白并扫描文本 surface。
  [`contracts.rs:178`](../../../crates/xtask/src/contracts.rs#L178)

- 独立 mutation 防止回归。
  [`generated_contract_gate_negative.rs:186`](../../../crates/xtask/tests/generated_contract_gate_negative.rs#L186)

**质量门证据**

- 12/12 FULL 与 PASS 决策。
  [`traceability-matrix.md:1`](../../tests/traceability-matrix.md#L1)
