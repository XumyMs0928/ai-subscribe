---
story: "4.1"
story_file: "_agentic-out/implementation/stories/4-1-将真实来源规范化为可溯源情报.md"
review_mode: full
delivery_profile: standard
status: pass_with_deferred_preexisting
reviewed_at: "2026-08-19T15:05:00+08:00"
---

# Story 4.1 Code Review

## 结论

Story 4.1 本次变更的 8 项 actionable findings 已全部修复，0 项未决；7 项由累积工作树暴露的 Story 2.5 既有生命周期问题已登记到 `deferred-work.md`，不冒充本 Story 新缺陷。5 项误报经源码路径核对后排除。

Story 保持 `in-progress`：本报告只关闭 code-review gate，standard profile 的 test-review、traceability 与最终合并门仍需完成。

## 已修复

1. 全部候选规范化失败时，来源进入稳定 format failure；不写 checkpoint/etag/cursor、不刷新 success time，并保存逐记录 failure projection。
2. checkpoint `last_seen_at_ms` 使用单调最大值，防止时钟回拨使 provenance `last_updated_at` 倒退。
3. URL literal 复用共享公网 IP policy，拒绝私网、多播、保留与文档网段。
4. provenance ID 使用完整 IntelItemId，不再以 96-bit 截断值配合 `INSERT OR IGNORE`。
5. v7→v8 在关闭 FK、开始重建前校验历史 result 的生命周期、hash、canonical time、HTTPS public URL 和字段规范；异常整笔拒绝。
6. RCL-002 更正为 Story Amendment，并保留用户批准和 AC→Task→Test 写回链。
7. automation manifest/latest JSON/summary 已统一到 Story 4.1；后续门禁明确保持 pending/stale。
8. 完整绿色计数改为 automation 前基线，不冒充 review 修复后的最终证据。

## 定向验证

- `all_invalid_candidates_fail_without_advancing_success_checkpoint`：PASS。
- `changed_fact_keeps_last_updated_monotonic_when_clock_moves_back`：PASS。
- `v7_result_backfill_rejects_noncanonical_or_unsafe_history`：PASS。
- `required_and_bounded_fields_fail_closed`（含 IPv4/IPv6 非公网 literal）：PASS。
- `final_fact_update_preserves_provenance_and_has_no_derived_projection`（完整 provenance ID）：PASS。
- `cargo clippy -p radar-core --all-targets -- -D warnings`：PASS。
- `cargo fmt --all`：PASS。

第一次 URL 定向测试发现 IPv6 host bracket 解析细节并已修正；第一次全无效来源断言使用了错误的数据库状态文案 `failed`，按既有 source enum 修正为 `error` 后只续跑失败项。

## Deferred（pre-existing）

- Story 2.5 retry_wait 缺生产重调度。
- mixed failed/retry_wait 与 Windows DTO 守卫不一致。
- retry_wait overall readiness 与 Windows 守卫不一致。
- budget timeout 后 detached fetch 可能继续。
- blocking sleep timer 占用线程。
- idempotency key TS/Rust 字符集不一致。
- budget exhaustion 被记录为 internal failure。

## 工作树说明

仓库自初始化提交后累积了多个 Story 的未提交用户工作，且本次修复与既有变更共享相同文件；为避免把无关用户改动一起 stage/commit，本审查没有创建局部提交。没有 reset、checkout、push 或全局工具安装。
