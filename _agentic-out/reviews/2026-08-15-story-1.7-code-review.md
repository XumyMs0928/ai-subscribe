# Story 1.7 Code Review

日期：2026-08-15  
范围：Story 1.7 最新 Playwright 生命周期修复与已批准范围调整增量  
结论：Approve

## Review Layers

- Blind Hunter：完成
- Edge Case Hunter：完成
- Acceptance Auditor：完成

## Triage

- Decision needed：0
- Patch：8
- Deferred：0
- Dismissed：1（跨平台 CI runner；当前 delivery scope 明确为 Windows-first）

## Resolved Findings

1. Playwright 进程增加 40 分钟 wall-clock 上限，超时终止受控进程树并返回非零。
2. Vite/Playwright 使用精确 PID 的 Windows tree kill；teardown 竞态不覆盖原始退出码。
3. 全部 Playwright 参数透传，兼容 pnpm 前导 `--`；`--list` 已验证只列出测试。
4. `BASE_URL` 固定为受控本地服务；继承恶意值的 P0 回归通过。
5. 每轮 E2E 先用项目内 TypeScript/Vite 构建当前源码，避免旧 `dist` 假绿。
6. `CI` 仅将 `1`/`true` 识别为真；`CI=false` 回归通过。
7. 移除已知会在 Windows 挂起的 Playwright 自管 webServer 默认路径。
8. Story/Sprint/manifest/Change Proposal/Completion Notes 状态语义已同步。

## Verification

- 完整 E2E：10/10，exit 0。
- P0：4/4，exit 0；在 `BASE_URL=http://127.0.0.1:9`、`CI=false` 下仍通过。
- P0+P1：10/10，exit 0。
- `test:e2e -- --list`：列出 10 项，不执行测试，exit 0。
- 故意无匹配测试：exit 1，项目 Node 残留 0。
- Node syntax、Prettier、ESLint、TypeScript：PASS。
- 独立最终项目 Node 残留：0。

## Remaining Gate

代码审查无未解决 finding。Story 保持 `review`，等待因上游范围调整而 stale 的 automation/traceability/NFR 质量制品重新生成；完成 required automation gate 后再进入 `done`。
