---
story: "4.2"
story_file: "_agentic-out/implementation/stories/4-2-确定性识别重复并关联同一事件来源.md"
review_mode: full
delivery_profile: standard
status: pass
reviewed_at: "2026-08-19T17:39:29+08:00"
---

# Story 4.2 Code Review

## 结论

三路 adversarial review 的 6 组 actionable findings 已全部修复，0 项未决、0 项延期；4 项与既定 fail-closed/后续 Story 边界冲突的建议已排除。后续 automation、test-review、traceability 与最终合并门均已通过，Story 已进入 `done`。

## 已修复

1. v9 migration 在事务 commit 前运行完整数据库、FK、schema 与 association verifier；失败整笔回滚并保持 v8。
2. association verifier 证明 eligible RSS provenance 恰有一个 current member，且 evidence ID 与当前 canonical URL 一致；缺失、额外、漂移及非 canonical 时间均 fail closed。
3. schema verifier 补齐 basis version/hash、member 时间 CHECK 与成员索引约束。
4. Unchanged 重放不创建新 group/member，但会按 checkpoint 单调刷新 member/group observation envelope。
5. reconcile 只更新旧、新受影响 association IDs，不再每个 item 全表刷新。
6. 新增 file-backed v9 reopen/corrupt、v8 migration rollback、双连接并发相反来源提交和 Unchanged observation 直接证据。

## 验证

- `cargo test -p radar-core --all-targets`：144/144 PASS（55 lib + 89 integration）。
- 新增 v9 file-backed/reopen/rollback：2/2 PASS。
- 新增并发 convergence：1/1 PASS。
- 新增 Unchanged observation：1/1 PASS。
- `cargo clippy -p radar-core --all-targets -- -D warnings`：PASS。
- `cargo fmt --all -- --check`：PASS。
- changed-file `git diff --check`：PASS。

全部命令均经项目内 `scripts/rust-env.cmd` 执行；未安装全局工具、未修改系统设置，也未重复运行未受影响的前端/Playwright 门禁。

## 排除项

- 历史非 canonical URL 在 v8→v9 migration 中 fail closed 是 AC6 明确要求，不做静默修复。
- online/backfill eligibility 在当前 RSS normalization 边界一致，不另造平行规则。
- association member 对 final fact 使用 `ON DELETE RESTRICT` 是当前保留事实与溯源的设计；用户删除生命周期属于后续 Story。
- migration 收集候选行的线性内存成本没有当前规模阈值证据；已修复可证明的逐 item 全表 envelope 更新。

## 工作树说明

仓库仍包含多个 Story 的未提交用户成果。本次只编辑 Story 4.2 关联切片及其审查制品，没有 reset、checkout、commit、push 或覆盖无关改动。
