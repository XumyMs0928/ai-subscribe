# Story 4.5 Code Review

## 结论

- **结果：PASS（代码审查阶段）**
- **范围：Windows-first、RSS/Atom-only 主情报流 minimum loop**
- **审查方式：** Blind Hunter、Edge Case Hunter、Acceptance Auditor 三份独立审查，统一去重、分类并按用户选择 `0` 批量修复。
- **发现：** 56 条原始意见，去重后 33 项；30 项 Patch 已关闭，3 项 Dismissed，未关闭 Patch 0 项。
- **状态：** 初始 33 项已关闭；post-fix follow-up 的 5 项 Patch 也已关闭，automation、test-review 与 traceability 已刷新。

## 主要修复

1. Core feed 只读取 current fact/rule/content/provenance projection，并以 effective time 与 evaluation time 双重固定 `as_of` 快照。
2. cursor 改为进程私有随机密钥 HMAC-SHA256，绑定查询身份；keyset tie-break 改用公开 `intel_item_id`，覆盖伪造、stale、limit 100 与跨页无重漏。
3. 50,000 行性能数据与固定时间窗一致；证据覆盖规范化 seed manifest、两类生产 SQL query plan、源码/候选 hash，并采用失效旧证据、临时写入、原子发布。
4. DesktopApi/Tauri transport 对 opaque track ID、时间、同分排序、projection metadata 和 filters 做语义级 fail-closed 校验。
5. Windows feed 补齐重要度多选、表单边界、显式刷新、stale cursor 恢复、跨页漂移/重复拒绝、互斥错误、健康与空态分层、Skeleton、焦点/选择/滚动 anchor 和 `/demo` 隔离入口。
6. browser mock 实现真实 stream、四维筛选、稳定排序与 cursor 分页；Vitest/Playwright 补齐 real root、分页、刷新、错误恢复、键盘与导航上下文证据。
7. contracts gate 结构化反序列化 intel-feed fixture，并将 application 层 `rusqlite` 例外收窄到 `#[cfg(test)]`；同时修正 time-window wire 枚举为 `last_24h` / `last_7d` / `last_30d`。

## 验证

- Rust 静态门：rustfmt、workspace all-target Clippy `-D warnings`、contracts gate 通过。
- Rust 测试：除 `ai-subscribe-desktop` 的零测试 `main.rs` 入口外，workspace all-target tests 全部通过；同包 8 个库测试通过。该入口由 Windows 应用控制策略阻止执行（OS error 4551），不是编译或测试断言失败。
- Windows 前端：Prettier、ESLint、typecheck、production build 通过；Vitest 130/130。
- Playwright：完整轮 34/35，唯一失败为新增用例定位器/未等待 focus 的测试缺陷；修正后仅复跑 Story 4.5，3/3 通过。
- 性能：仓库 runner 显式执行唯一 ignored AC7 gate；固定 50,000 行、每场景 30 样本，current default P95=62.021ms、combined P95=100.734ms，均低于 200ms。
- 性能证据：`target/story-4-5/intel-feed-performance-35400-0.json`，含 dataset/candidate/source hash、原始样本、SQLite/硬件与两类完整 query plan；runner 要求恰好一个 fresh evidence 并验证合同。

## Post-fix Follow-up

- 三层 follow-up 共保留 5 项 Patch、Dismissed 4 项；5 项已全部关闭：显式 AC7 runner、invocation-scoped 原子 evidence reservation、原生按钮语义断言、完整 AC→Task→Test/evidence 映射、automation/traceability 刷新。
- test-review 复审又发现并修复 `window.scrollBy` spy 泄漏与 runner 魔法合同值；当前质量 96/A，0 Critical/High、3 Medium、1 Low，结论 Approve with Comments。
- Traceability：AC1–AC7 7/7 FULL；P0/P1/overall 100%；deterministic gate PASS，blocker=0。
- 受影响回归：reservation 1/1、Vitest 130/130、Story Playwright 3/3、rustfmt、radar-core Clippy `-D warnings`、Prettier/ESLint/typecheck 与 isolated AC7 runner 均通过。
- 未创建 workflow commit：工作树包含大量用户既有及其他 Story 的未提交/未跟踪改动，无法安全隔离本 Story 提交；未 stage、reset 或改写用户变更。

## 范围声明

本结论仅覆盖 Windows RSS minimum loop。处理状态、收藏、全文搜索、真实详情/原文、AI、通知、GitHub/arXiv、移动端与完整跨平台性能矩阵继续 Deferred。

> Flow sync（2026-08-20）：此报告之后的 Story 文件变更仅写回 test-review/flow 元数据，没有产品代码或 code-review 范围变化；本结论仍为 complete。

> Final reconciliation：Story 4.5 进入 done 后再次核对，30 个 Patch 项与 5 个 follow-up Patch 项仍全部关闭，code-review gate 保持 **PASS**。
