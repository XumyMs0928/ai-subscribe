---
stepsCompleted: ['step-02-discover-tests', 'step-03f-aggregate-scores', 'step-04-generate-report']
lastStep: 'step-04-generate-report'
lastSaved: '2026-08-19'
story: '4.1'
scope: 'Windows RSS/Atom minimum loop'
---

# Story 4.1 Test Review

## Scope and strategy

- Review target: Story 4.1 normalization, v8 migration, atomic sync persistence, and additive DesktopApi transport compatibility.
- Stack: Rust unit/integration tests plus Vitest transport contract regression. Story 4.1 adds no UI route or E2E journey, so existing Playwright Story 2.6 coverage is regression context rather than a newly claimed acceptance layer.
- Execution policy: static review first; only targeted tests after a concrete fix; one full combined gate after review and traceability converge.
- Coverage mapping and gate decisions are intentionally deferred to `agentic-test-traceability`.

## Discovered tests

| File | Lines | Framework | Tests | Notes |
| --- | ---: | --- | ---: | --- |
| `crates/radar-core/tests/intel_normalization.rs` | 206 | Rust test | 5 | Pure fixed factories; normalization, field bounds, serde boundary, legacy wire compatibility. |
| `crates/radar-core/tests/sync_tasks.rs` | 1053 | Rust integration | 19 | Scoped file DB uses PID + atomic sequence and Drop cleanup; fixed timestamps; no hard waits or live network. |
| `crates/radar-core/src/application/demo.rs` | 2969 | Rust inline unit/migration | 16 | Scoped DB helpers; embedded fixture; includes v7→v8 backfill and corrupt-history rejection. |
| `crates/radar-core/src/application/sync.rs` | 2804 | Rust inline unit/integration | 8 | In-memory transactions and fault injection; fixed candidate data; no waits/live network. |
| `apps/windows/src/lib/desktop-api/tauri-desktop-api.test.ts` | 1295 | Vitest | 26 | One describe group; mocked Tauri invoke; shared DTO factories; exact guard mutation tests. |

Total scoped cases: 74 across five files. No ignored/skipped tests, real external calls, random data, sleeps, or hard wait primitives were found in this scope.

## Parsed metadata observations

- Priorities are not embedded in Rust names; risk is traceable through Story AC/task mapping and the automation report. Existing Playwright tests retain explicit `[P0]/[P1]` markers.
- `sync_tasks.rs` has one apparent 141-line marker interval, but lines 400–443 are a reusable `sync_one` helper; the actual preceding test body is under 100 lines.
- Production `SystemTime` appears in `application/sync.rs`, but scoped tests inject or assert fixed values; it is not a test nondeterminism source.
- Fixtures/factories are deterministic: Rust constructors use fixed IDs/timestamps/content; Vitest imports shared sync factories and resets the mocked `invoke` in test setup.
- Browser evidence collection is not applicable to this backend-first delta: no Story 4.1 UI/route behavior changed, and browser mock evidence cannot validate SQLite migration or transaction semantics.

## Executive summary

**Final quality score: 100/100 (A). Recommendation: Approve.**

The initial four-dimensional review scored 90/A because of five maintainability-only findings. All five were refactored and independently re-reviewed: repeated arrangements now use helpers/factories, and two multi-concern tests were split. There are no remaining determinism, isolation, maintainability, or performance violations.

Coverage is intentionally not scored here; Story 4.1 AC coverage and the release gate belong to `agentic-test-traceability`.

## Quality evaluation

Initial weighted score: **90/100 (A)**. Post-fix weighted score: **100/100 (A)**.

| Dimension | Score | Grade | Violations |
| --- | ---: | --- | ---: |
| Determinism | 100 | A | 0 |
| Isolation | 100 | A+ | 0 |
| Maintainability | 100 | A | 0 |
| Performance | 100 | A+ | 0 |

Remaining violations: 0. No runtime correctness, isolation, determinism, maintainability, or performance defect remains in scope.

Closed refactoring targets:

1. Replace repeated normalization error extraction with a table-driven assertion helper.
2. Reuse existing Rust sync setup helpers and the TypeScript sync result factory.
3. Split two multi-concern tests so failures remain independently diagnosable.

## Quality criteria

| Criterion | Status | Notes |
| --- | --- | --- |
| Determinism / flake resistance | PASS | Fixed times and deterministic IDs; no hard waits, live network, or order dependency. |
| Isolation / cleanup | PASS | In-memory DB or PID+atomic scoped DB with Drop cleanup; Vitest invoke mock reset. |
| Fixture and factory use | PASS | Shared Rust helpers and TypeScript sync factories used after refactor. |
| Explicit assertions | PASS | Domain, migration, transaction rollback, DTO exactness, and legacy compatibility asserted directly. |
| Test length | PASS | Every reviewed test body is below 100 lines after split. |
| Performance | PASS | No serial suite, hard wait, excessive navigation, or unnecessary large fixture setup. |
| Network-first | N/A | Story 4.1 tests do not perform browser or live network operations. |
| Coverage | OUT OF SCOPE | Routed to traceability as required by the workflow. |

## Closed findings

1. `intel_normalization.rs`: extracted one normalization issue assertion helper.
2. `application/sync.rs`: reused `save_test_source` and `start_and_claim` in corruption/rollback tests.
3. `tauri-desktop-api.test.ts`: reused `createSyncResultPage` and `encodeSyncResultCursor`.
4. `sync_tasks.rs`: split optional-time normalization from same-hash replay behavior.
5. `application/demo.rs`: split summary field exclusion from cursor behavior.

## Targeted validation

- Rust normalization integration: 5/5 passed.
- Rust sync integration: 20/20 passed.
- radar-core library tests: 46/46 passed.
- Vitest: 113/113 passed; TypeScript typecheck passed.
- rustfmt and changed-file Prettier checks passed.
- The gnullvm executable was compiled but Windows application control blocked its launch (OS 4551). The same tests passed via the repository-local MSVC toolchain; no system policy was changed.

## Best practices retained

- Scoped database paths combine process ID and atomic sequence and clean WAL/SHM sidecars in Drop.
- Migration tests use fixed historical rows and deterministic tie-breaks.
- Transaction tests inject failures at specific persistence stages and verify full rollback plus recovery.
- DesktopApi tests fail closed on malformed additive and legacy wire shapes.

## Context and references

- Story: `_agentic-out/implementation/stories/4-1-将真实来源规范化为可溯源情报.md`
- Framework: `playwright.config.ts`, root/app `package.json`, workspace `Cargo.toml`
- Consulted guidance: `test-quality.md`, `data-factories.md`, `test-levels-framework.md`, `selective-testing.md`, `test-healing-patterns.md`, `selector-resilience.md`, `timing-debugging.md`, Playwright Utils fragments, and `contract-testing.md`.
- Persisted dimension evidence: `_agentic-out/tests/reports/test-review-workers/story-4-1-quality-dimensions.json`.

## Decision and next workflow

**Approve.** No critical or advisory test-quality finding remains. Run `agentic-test-traceability` next, then execute the single final combined gate on the converged worktree.
