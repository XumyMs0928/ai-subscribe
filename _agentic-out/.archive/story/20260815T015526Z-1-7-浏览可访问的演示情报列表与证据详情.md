---
artifact_kind: story
status: in-progress
delivery_profile: standard
delivery_scope: windows-first
source_story: ''
baseline_commit: f73aed0
---

# Story 1.7：浏览可访问的演示情报列表与证据详情

状态：in-progress

## Story

作为首次使用者，  
我希望在 Windows 原生界面中浏览演示情报并查看清晰、可访问的证据详情，  
以便理解产品如何把信息转化为可核验判断。

## 验收标准

### AC1：稳定的 Windows 列表—详情浏览

**Given** 演示情报已经加载  
**When** 用户通过鼠标或键盘选择一条列表记录  
**Then** Windows 在宽布局中原位更新详情，在空间不足时进入独立详情视图或 Sheet  
**And** 当前选择、列表焦点、搜索/筛选条件及列表滚动锚点保持稳定；切换记录只将详情回到顶部，不滚动列表。（FR2、UX-DR2、UX-DR11、UX-DR12）

### AC2：从判断到证据的固定信息层级

**Given** 用户打开演示情报详情  
**When** 首个详情视口完成渲染  
**Then** 按“来源与标题、发生了什么、为什么重要、可能影响、重要程度、AI 置信度、主要操作”的顺序展示  
**And** 原始事实、规则判断、示例 AI 内容和溯源分别使用语义标题/region 与“演示数据”“AI 生成”等文字或图标标识，不只依赖颜色；缺失字段显示“未提供”。（UX-DR3、UX-DR4、NFR29）

### AC3：Windows 设计系统与自适应布局

**Given** Windows 已实现列表与详情  
**When** 检查视觉和响应式实现  
**Then** 使用现有 React、shadcn/ui、Tailwind CSS 与适用的 Radix 原语，页面不是 Card 墙  
**And** 颜色、字体、间距、边框、焦点、运动和反馈均引用 `DESIGN.md` 语义 token，不在页面硬编码视觉值；1024×700 最小窗口及适用窄宽度下核心内容和操作仍可达。（UX-DR1、UX-DR22）

### AC4：键盘与 Windows 屏幕阅读器可用

**Given** 用户仅使用键盘或 Windows 屏幕阅读器  
**When** 选择条目、进入详情、遍历事实/规则/AI/溯源区块并返回列表  
**Then** 核心操作具有稳定的可访问名称、角色、状态、可见焦点和与视觉一致的遍历顺序  
**And** 上/下方向键及 J/K 移动选择，Enter 将焦点进入详情，Esc 返回已选列表项，`/` 聚焦搜索；输入控件内不截获快捷键，无焦点陷阱，所有快捷操作均有可见等价入口。（UX-DR20、UX-DR21、UX-DR24、NFR30 scoped evidence）

### AC5：主题、缩放和关键状态可复现

**Given** 组件处于浅色/深色主题及 default、focus、selected、loading、empty、error/retry 状态  
**When** 执行 Windows 场景和真实候选构建检查  
**Then** `IntelligenceFeedItem`、`EvidenceDetailPanel` 与 `SourceProvenanceGroup` 的关键状态均可稳定复现  
**And** Windows 100%、125%、150%、175%、200% × 浅/深主题的 10 个组合中，阻断阅读/操作的裁切与重叠为 0；高对比度和 Reduce Motion 保留状态含义与可见焦点；HTML 原型、截图或组件测试不冒充原生运行证据。（UX-DR19、UX-DR21、UX-DR23、NFR31）

### 当前 Windows-first 验收覆盖

- 本轮实施并验收 AC1–AC5 的共享合同与 Windows 适用部分。
- 原 Epic 中紧凑移动详情栈、宽屏平板 split view、Apple SwiftUI/VoiceOver/Dynamic Type/Xcode Preview、Android Compose/TalkBack/48dp/Compose Preview 条款原文保留为 Deferred Acceptance；不删除、不标 PASS，也不阻塞 Windows milestone。
- 本 Story 完成只表示 Windows milestone 完成，不表示跨平台 Story 或 MVP 完成。
- NFR30/NFR31 本轮只产生主情报流、搜索、详情与相关错误界面的 scoped evidence；未实现的收藏、设置和平板键盘路径不据此标记 overall PASS。

### Deferred Acceptance（原 Epic 条款，deferred / not executed / not PASS）

- AC1：紧凑移动布局进入详情栈，宽屏平板使用原生列表—详情布局；当前选择、焦点和列表滚动状态保持稳定。
- AC3：Apple 使用 SwiftUI，Android 使用 Compose Material 3 Adaptive；三端共享语义和状态而非 UI 代码。
- AC4：Apple 支持 VoiceOver/Dynamic Type；Android 支持 TalkBack 且核心触控目标不小于 48×48dp。（NFR43、NFR44）
- AC5：执行 Xcode Preview 和 Compose Preview，覆盖浅/深主题与默认、焦点、选中、加载、空和错误状态；HTML 原型不作为原生运行证据。

## Tasks / Subtasks

- [ ] 1. 扩展唯一权威 demo 证据合同（AC2、AC5）
  - [ ] 在 `contracts/fixtures/demo/manifest-v1.json` 就地演进版本化样本，使每条记录权威承载 `what_happened`、`why_it_matters`、`possible_impact`、importance、AI confidence、事实、规则、示例 AI 和 provenance；不得由 React 拼装或复制平台 fixture。
  - [ ] provenance 至少权威承载 `source_kind`、`publisher`、`author?`、`original_title`、`original_url`、`published_at?`、`collected_at`、`first_discovered_at`、`last_updated_at`、`availability_status`、`deterministic_association_basis?`；缺失作者/时间显示“未提供”，原文失效显示明确状态。
  - [ ] 在 `radar-core` 的 demo 领域/DTO/SQLite 投影中同步严格校验与持久化；字段使用 snake_case、显式 null、RFC3339 UTC、不透明 ID，事实/规则/AI 分层且示例 AI 永不触发 provider、凭据、网络、通知或指标。
  - [ ] 将轻量列表 `DemoIntelSummaryV1` 与按 ID 返回的版本化 `DemoEvidenceDetailV1` 分开；每个独立 wire DTO 都携带 `contract_version`，列表不得携带完整 evidence、AI 输入或正文。
  - [ ] 通过下一单调、事务化 SQLite migration 将原始事实/provenance、规则判断与示例 AI 投影分层保存；不得继续把所有详情字段堆入 `intel_items`，升级/重启/重复 seed 必须幂等且可恢复。
  - [ ] 决定兼容策略并保持单一 `contract_version` 语义：若现有 v1 字段不兼容则显式升级并同步 schema/snapshot/golden；不得静默改变 v1 含义。
  - [ ] 同步 Tauri command wire、`DesktopApi` DTO/guard、Playwright factory/mock 和 xtask drift/mutation gate；未知 enum/缺失必需字段必须受控失败，不映射为成功。

- [ ] 2. 建立可复用领域组件与状态模型（AC2、AC3、AC5）
  - [ ] 在现有 `apps/windows/src/features/demo-intelligence/` 内演进并提取 `IntelligenceFeedItem`、`EvidenceDetailPanel`、`SourceProvenanceGroup`；不建立平行 feature、跨平台 DOM 包或泛化 common/service 目录。
  - [ ] Feed item 展示标题、发布方/来源、时间/新鲜度、一行关注原因、赛道、重要度及演示/AI 状态；使用语义列表条目，不使用 Data Table/Card 墙。
  - [ ] Detail 固定实现 AC2 顺序，正文宽度和区块层级遵循 DESIGN；事实、规则、AI、溯源可按 heading/region 导航，缺失与不可用状态有明确文字，并逐字段呈现 provenance。
  - [ ] 用统一 render-state 表达 initial loading、refreshing with data、ready、empty、offline cached、AI waiting/failed、局部错误、blocking error 与 retry；刷新/详情错误保留已有数据，错误使用稳定 `message_key`/code，绝不展示底层私密文本。
  - [ ] 空状态说明原因、当前条件、数据新鲜度和下一步；错误说明对象、阶段、时间、影响、数据安全、系统下一步和用户动作。刷新后选中项消失时说明原因、选择相邻项并恢复合法焦点。

- [ ] 3. 实现稳定选择、焦点和响应式布局（AC1、AC3、AC4）
  - [ ] 用稳定 item ID 管理 selection；异步领域数据仅由 TanStack Query 管理，Query key 继续集中生成，纯 UI 焦点/面板状态才允许进入 local state（需要跨组件时才使用 Zustand）。
  - [ ] 查询切换使用 TanStack Query v5 `placeholderData`/`keepPreviousData` 或等价缓存策略保留已有列表/详情；保持 10 秒 timeout、请求代次保护和 stale completion 防护。
  - [ ] 将列表/筛选查询改为 core 生成和校验的不透明 keyset cursor，按稳定 `(published_at DESC, external_id ASC)` 排序并以 SQL `LIMIT + 1` 分页；UI 不解析 cursor，禁止全 catalog 载入后内存切片或暴露 `offset:n`。
  - [ ] 把触达 SQLite 的 Tauri command 改为 async 外观并交由受控 blocking executor/专用数据库边界执行，保持串行写和有界读取，不阻塞 WebView 或 async runtime；失败后 store 仍可用。
  - [ ] 列表拥有独立滚动容器和滚动锚点；宽布局原位更新，窄布局/Sheet 返回时恢复选中项、焦点、滚动与筛选；切换详情将详情容器回顶部。
  - [ ] 响应式断点固定为：`≥1280px` 完整三栏、`1024–1279px` 压缩三栏、`640–1023px` 导航+列表且详情进入独立视图/Sheet、`<640px` 单栏；任何切换继续保留搜索、筛选、选择、焦点和滚动锚点。
  - [ ] 采用适合内容结构的语义：若条目内含多个可交互控件，使用原生 list + 单一主选择按钮及独立工具栏；只有满足 WAI-ARIA APG 扁平 option 模型时才用 listbox/option。仅 listbox/option 使用 `aria-selected`；原生 list + button 使用 `aria-current="true"` 或可访问选中说明并配合 roving tabindex，不把 `aria-selected` 写到 button/listitem。焦点与选中视觉必须不同。

- [ ] 4. 完成键盘、读屏与非颜色语义（AC2、AC4）
  - [ ] 实现上/下方向键、J/K、Enter、Esc、`/`；快捷键不在 input/textarea/select/contenteditable 内截获，不实现尚未交付的 O/S/F 假操作。
  - [ ] 列表项 accessible name 稳定包含标题、来源及必要状态；动态结果使用克制的 `aria-busy`/polite live，错误与 retry 程序化关联，禁止重复打断播报。
  - [ ] 详情首个焦点目标、heading 层级、region 名称和 DOM/视觉顺序一致；返回精确恢复已选列表项，无 keyboard trap。
  - [ ] 所有 demo/AI/importance/error/selected 状态同时有文字、图标、边框或形状等非颜色表达；截断内容提供键盘可达的完整文本入口，不只依赖 Tooltip。

- [ ] 5. 完善 Windows token、主题、缩放与状态场景（AC3、AC5）
  - [ ] 复用并按 `DESIGN.md` 补齐 globals 语义 token；Segoe UI Variable、4px 网格、2px focus ring + 2px offset、列表/详情无装饰阴影；不得在页面 JSX/CSS 写一次性颜色、间距或动效常量。
  - [ ] selected 使用背景 + 左侧 2px 指示 + 可访问状态；forced-colors 下使用系统色/边框且 focus/selected/error/demo/AI 仍可区分；Reduce Motion 将非必要动画归零。
  - [ ] 建立自动化可进入的 Windows 场景/路由或测试 harness，覆盖 default/focus/selected/loading/empty/error/retry、长标题、缺失 provenance、AI unavailable 及浅/深/forced-colors；场景不得进入生产导航或成为第二数据源。
  - [ ] 校验 1024×700 最小窗口以及 100%–200% 缩放下无横向溢出、核心裁切、重叠或不可达操作；空间不足时重排/滚动而非缩小文字或隐藏能力。

- [ ] 6. 建立自动化与真实 Windows 验收证据（AC1–AC5）
  - [ ] Rust/contract tests 覆盖新增 evidence schema 的合法/非法/未知字段、seed/reseed/迁移、事实/规则/AI 分层及 Story 1.6 零外联回归。
  - [ ] 分页测试覆盖首/中/末页无重复无遗漏、非法/伪造 cursor、limit 0/101、稳定恢复；直接断言 summary 不含完整 evidence 且 detail 拥有独立 contract version。
  - [ ] Vitest/Testing Library 覆盖组件语义、heading 顺序、合法 role—ARIA 属性、focus/selected 分离、键盘、scroll preservation、stale response、loading/refresh/empty/error/retry、主题与 forced-colors class/token；避免只断言源码子串。
  - [ ] Playwright 覆盖真实浏览旅程：加载→列表选择→详情证据层级→搜索/筛选→切换/返回，并断言选择、焦点、滚动、可访问状态与 external calls=0；逐测试隔离且无硬等待。
  - [ ] 使用 MSVC release 候选执行 UIA/键盘 smoke；真实 NVDA 验收遍历列表和事实/规则/AI/溯源区块，并记录 OS、WebView2、NVDA、窗口、缩放、主题、高对比度、输入方式、预期/实际及证据位置。
  - [ ] 完成 NFR31 中本 Story 所有列表、搜索、详情及错误界面的 10 个缩放×主题组合及 forced-colors/Reduce Motion 检查；只记录 scoped evidence，不据此标记 NFR31 overall PASS。任何阻断裁切、重叠、焦点丢失或读屏无法完成路径均阻塞 done。
  - [ ] 执行项目隔离的 frozen install、format/lint/typecheck、Vitest、Playwright、Vite build、Rust fmt/Clippy/workspace tests、xtask contracts、MSVC release 与 burn-in；新候选必须重新运行 Story 1.6 的 30 次启动样本，不沿用旧候选证据。

## Dev Notes

### 强制数据流

```text
contracts/fixtures/demo（唯一证据事实）
  → radar-core validation / SQLite / Application queries
  → approved Tauri commands
  → DesktopApi runtime guards
  → TanStack Query
  → Windows presentation models
  → IntelligenceFeedItem / EvidenceDetailPanel / SourceProvenanceGroup
```

### 实现护栏

- 先扩展共享证据合同，再做 UI；UI 不推断 ID、不重新计算重要度/置信度，也不生成事实、规则或 AI 文本。
- 继续使用 `View → Provider → DesktopApi → Tauri command → radar-core`；只有 `tauri-desktop-api.ts` 可调用 `invoke`，React/Tauri 不访问 SQLite。
- 当前 demo feature、fixture、SQLite/FTS、commands、guards、query client、Playwright fixture 与零外联探针均须复用。新增 command 必须精确加入 release allowlist；不要引入通用 execute、shell/fs/http 权限。
- WAI-ARIA listbox 的 option 是扁平名称模型且不适合包含可交互后代；开发者必须按实际条目结构选择原生 list/button 或真正合规的 listbox，不能只换 role。
- 焦点是 `document.activeElement`，选中是持久语义状态；键盘移动、视觉 focus ring 和 `aria-selected` 必须分别测试。
- TanStack Query v5 通过 `placeholderData`/`keepPreviousData` 保留查询切换时的旧数据；仍须保留旧错误和刷新状态语义，不能把 placeholder success 当作真实新请求成功。
- 所有安装、缓存、浏览器、Node/pnpm、Rust/Cargo、Python 和测试数据只能位于项目目录；不得修改全局 Python、PATH、主题、缩放、高对比度或系统包。

### 项目结构与范围边界

- 优先在 `apps/windows/src/features/demo-intelligence/` 就地演进；只有组件语义稳定且被多个 feature 复用后才迁到 `intel-feed`/`intel-detail`。禁止保留旧/new 两套事实模型。
- Story 1.7 不实现 Story 1.8 的配置引导，也不实现真实 RSS/GitHub/arXiv、真实 AI/provider/Key、通知、同步、收藏、反馈、处理状态、外部浏览器、托盘/单实例或移动端 UI。
- “主要操作”只呈现本 Story 已有的真实浏览/返回/重试等能力；O/S/F、收藏、反馈、打开原文不得做空壳或假成功。
- 标准交付 profile 是用户“快速完成 Windows”方向下的显式覆盖；风险评估推荐 assured，但本 Story 仍把真实 Windows NVDA、缩放/主题和候选构建证据保留为硬门禁。
- 当前仓库基线为 `f73aed0`，工作区已有用户变更；实现不得重置或覆盖无关修改，diff/File List 归属须逐文件核验。

### 测试完成判定

- 自动化、HTML、组件场景、截图和源码扫描可用于回归，但不能替代真实 Tauri/MSVC 候选的 NVDA、焦点、主题和缩放证据。
- 不允许 ignored/skipped/条件早退冒充覆盖。系统条件无法执行时必须记录 blocker/partial，Story 保持 `in-progress`。
- 新 release candidate 会使 Story 1.6 旧 benchmark 的 source provenance 失效；必须为新候选重新采集 30 次，并绑定 candidate/source SHA。
- 完成前运行 agentic-code-review、agentic-test-automation、agentic-test-review、agentic-test-traceability；结论仅限 Windows milestone。

## Previous Story Intelligence

- Story 1.6 已交付 3 条固定 demo、SQLite/FTS、bootstrap/list/detail/search/filter、TanStack Query、6 个窄 Tauri commands、DesktopApi guard、Playwright fixture、零网络/AI/通知证据及 Windows 30 次启动基线。
- 当前 `DemoItemV1` 仅有 publisher/title/track/summary/url/timestamps，不能承载 AC2；本 Story 必须从唯一 fixture 向 core/IPC/TS 同步扩展。
- 当前同一 `DemoItem` 同时充当列表/详情、详情无独立 `contract_version`，`list_page` 又全量加载后使用 `offset:n`；本 Story 必须修正为 summary/detail 分离、版本化详情和 core-owned keyset pagination。
- Story 1.6 最终回归为 Rust 50、Vitest 19、Playwright 7；新实现不得减少既有覆盖或放宽 xtask/release boundary。
- 审查经验：安全/可访问证据必须穿过真实语义，不能用源码子串、扁平 UIA 文本或 fixture 自报值冒充；测试临时数据库使用唯一 scoped path + RAII，长 mutation case 使用 helper/表驱动，E2E locator/helper 复用且逐测试隔离。
- Story 1.6 候选 SHA 与 30 次证据仅对当时二进制有效；本 Story 修改生产源/lock 后必须重建重采。

## References

- [Source: `_agentic-out/planning/epics.md`，Story 1.7、Windows-first delivery overlay、UX-DR1–4/11/12/19–24]
- [Source: `_agentic-out/planning/prd.md`，FR2、NFR29–31、NFR43–44]
- [Source: `_agentic-out/planning/architecture.md`，feature 结构、DesktopApi、TanStack Query、Render State、契约与平台边界]
- [Source: `_agentic-out/planning/ux/DESIGN.md`，Windows tokens、布局、主题、焦点、缩放与状态]
- [Source: `_agentic-out/planning/ux/EXPERIENCE.md`，情报列表、详情证据、键盘、辅助技术和恢复语义]
- [Source: `_agentic-out/planning/ux-design-specification.md`，IntelligenceFeedItem、EvidenceDetailPanel、SourceProvenanceGroup]
- [Source: `_agentic-out/implementation/stories/1-6-浏览安全隔离的演示情报.md`，已实现资产、测试与 review lessons]
- [Official: `https://www.w3.org/WAI/ARIA/apg/patterns/listbox/`，listbox/option、键盘和 aria-selected 约束]
- [Official: `https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/`，焦点与选中状态分离]
- [Official: `https://tanstack.com/query/latest/docs/framework/react/guides/paginated-queries`，v5 previous-data/placeholderData]
- [Official: `https://v2.tauri.app/develop/calling-rust/`，Tauri command 注册、参数和 serde 返回]
- [Official: `https://learn.microsoft.com/windows/apps/design/accessibility/high-contrast-themes`，Windows Contrast Themes 运行检查]

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- 2026-08-15：通过 agentic-flow 选择 Story 1.7；用户目标为快速完成 Windows 功能，移动端继续延期，所有安装严格项目隔离。
- 2026-08-15：delivery profile 风险建议为 assured；依据用户速度优先方向对本 Story 显式采用 standard，但不降低 Windows NVDA、缩放/主题与候选构建门禁。
- 2026-08-15：并行复核 Epic/PRD、architecture/current source 与 UX/Story 1.6；确认首要缺口是 demo evidence DTO 无法表达完整证据层级。

### Completion Notes List

### File List

- `_agentic-out/implementation/stories/1-7-浏览可访问的演示情报列表与证据详情.md`

## Requirement Change Log

### 2026-08-15 — Windows-first 交付覆盖

- **Trigger：** 用户明确要求优先快速落地全部 Windows 功能，将手机和平板实现延后；该方向已写入 Epic Windows-first overlay 和 sprint tracking。
- **Classification：** Cross-artifact Change（已在本 Story 创建前完成上游批准与写回）。
- **Previous behavior：** Story 1.7 同时要求 Windows、Apple/iPhone/iPad 与 Android 手机/平板实现和原生运行证据。
- **New behavior：** 本 milestone 只实施共享合同与 Windows UI/运行证据；移动端原验收条款保留为 Deferred Acceptance，不阻塞 Windows done，未来恢复时继续使用同一合同语义。
- **Acceptance Criteria affected：** AC1、AC3、AC4、AC5 的平台覆盖；AC2 共享证据语义不变。
- **Tasks affected：** Tasks 1–6 均按 Windows-first 编排；移动平台 tasks 不在本轮创建。
- **Upstream artifacts affected：** `_agentic-out/planning/epics.md`、`_agentic-out/implementation/sprint-status.yaml` 已包含 Windows-first overlay；PRD/architecture 的最终跨平台目标保持不变。
- **Tests required：** Windows contract/component/E2E/MSVC/NVDA/缩放主题证据；Apple/Android 证据延期且不得标 PASS。
- **Approval evidence：** 用户先后明确表示“目前我只想先实现windows端的功能，手机平板的先保留先不实施”“快速落地所有才把其余两端的延后的”，并多次批准 Windows-first 调整。
- **Status：** applied。
